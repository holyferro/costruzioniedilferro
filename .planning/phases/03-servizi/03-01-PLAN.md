---
phase: 03-servizi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/services.ts
autonomous: true
requirements:
  - SRV-01
  - SRV-02
  - SRV-03
  - SRV-04
  - SRV-05
must_haves:
  truths:
    - "Il content module `content/services.ts` esporta tutto il copy della pagina /servizi (hero, 3 target, 4 step processo, finalCta) — zero stringhe italiane saranno hardcoded nei componenti"
    - "I tre target hanno gli id ancora canonici 'privati', 'pubblico', 'professionisti'"
    - "La sezione Enti Pubblici (id 'pubblico') include SOA badges per OG1, OG2, OG3 con descrizione in italiano leggibile"
    - "I servizi descritti coprono i 4 mandatori: nuove costruzioni, ristrutturazioni di pregio, opere pubbliche, urbanizzazioni"
    - "Le immagini referenziate puntano a file esistenti in public/images/design/ (img-residenziale.webp, img-pubblico.webp, img-industriale.webp)"
  artifacts:
    - path: "content/services.ts"
      provides: "ServicesContent data + tipi (ServicesTarget, SoaBadge, ProcessStep, ServicesContent) + export servicesContent as const"
      contains: "export const servicesContent"
      contains_also:
        - "export type ServicesTarget"
        - "export type SoaBadge"
        - "export type ProcessStep"
        - "export type ServicesContent"
        - "OG1"
        - "OG2"
        - "OG3"
        - 'id: "privati"'
        - 'id: "pubblico"'
        - 'id: "professionisti"'
        - "Richiedi un sopralluogo"
        - "Urbanizzazion"
  key_links:
    - from: "content/services.ts"
      to: "consumed by ServicesHero, TargetIndex, ServicesEditorialRow, HowWeWork, HomepageCta in plan 02 + 03"
      via: "named export servicesContent + type exports"
      pattern: "export const servicesContent"
---

<objective>
Creare il content module `content/services.ts` — fonte unica di tutto il testo, dati e configurazione per la pagina `/servizi`. Definisce i tipi consumati dai 4 nuovi componenti di sezione (Plan 02) e dal page orchestrator (Plan 03).

Purpose: La regola di Phase 3 (D-12) e di Phase 1 (content-layer-first) impone zero stringhe italiane hardcoded nei componenti. Il content module è il prerequisito di interface per tutti i componenti della pagina — viene scritto per primo per evitare la "scavenger hunt" di Plan 02 e Plan 03.

Output: `content/services.ts` esistente, type-safe, importabile, contenente tutto il copy definitivo della pagina /servizi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-servizi/03-CONTEXT.md
@.planning/phases/03-servizi/03-UI-SPEC.md
@.planning/phases/03-servizi/03-PATTERNS.md
@content/homepage.ts
@CLAUDE.md

<interfaces>
<!-- Type pattern reference from content/homepage.ts (lines 6-109) — replicate the readonly + as const conventions exactly. -->
<!-- Phase 3 reuses kicker strings from homepageContent.services.items for cross-page consistency. -->

From `content/homepage.ts`:

```typescript
// Reference type (Phase 2) — Phase 3 ServicesTarget extends this shape with id + soaBadges.
export type ServiceItem = {
  readonly n: string;
  readonly kicker: string;
  readonly title: string;
  readonly body: string;
  readonly imageSrc: string;
  readonly imageAlt: string;
  readonly tags: readonly string[];
  readonly ctaLabel: string;
  readonly ctaHref: string;
};
```

Canonical kicker strings to reuse (from `content/homepage.ts` lines 159, 175, 187):

- `"Privati"` (item 0 — matches Phase 2 homepage.ts line 159)
- `"Settore Pubblico"` (item 1 — matches line 175)
- `"Aziende & Professionisti"` (item 2 — matches line 187)

Canonical primary CTA string (from `content/homepage.ts` line 293):

- Primary: `{ label: "Richiedi un sopralluogo", href: "/contatti" }`
- Secondary: `{ label: "Richiedi un preventivo", href: "/contatti" }`

Existing image files in `public/images/design/`:

- `img-residenziale.webp` → Privati
- `img-pubblico.webp` → Enti Pubblici
- `img-industriale.webp` → Professionisti / Aziende
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create content/services.ts with all types and copy</name>
  <files>content/services.ts</files>

<read_first> - content/homepage.ts (entire file — replicate exact type-readonly conventions, kicker strings, CTA labels, structural patterns) - .planning/phases/03-servizi/03-UI-SPEC.md (Copywriting Contract section — locks canonical CTA labels, eyebrow strings; SOA Badge Specification — locks badge data shape) - .planning/phases/03-servizi/03-PATTERNS.md (`content/services.ts` section lines 379-461 — full type signatures and export pattern) - .planning/phases/03-servizi/03-CONTEXT.md (D-01 hero copy options, D-07 services per target, D-08 SOA categories, D-09 4 process steps, D-11 tone of voice, D-12 zero hardcoded strings rule)
</read_first>

  <behavior>
    Il file `content/services.ts` deve:
    - Compilare con TypeScript strict (`pnpm typecheck` passa) senza errori
    - Esportare 4 tipi (ServicesTarget, SoaBadge, ProcessStep, ServicesContent) tutti `readonly`
    - Esportare la costante `servicesContent: ServicesContent` con suffisso `as const`
    - Contenere zero `import` (stesso pattern di content/homepage.ts — pure TypeScript)
    - Contenere stringa `"Richiedi un sopralluogo"` per il primary CTA
    - Contenere id `"privati"`, `"pubblico"`, `"professionisti"` esatti
    - Contenere i 3 codici SOA `"OG1"`, `"OG2"`, `"OG3"` come campo `code` di SoaBadge
    - Contenere riferimento a tutti e 4 i servizi mandatori (nuove costruzioni, ristrutturazioni di pregio, opere pubbliche, urbanizzazioni) attraverso le `tags` o body delle 3 sezioni target
    - Contenere 4 ProcessStep con n "01"/"02"/"03"/"04" per "Come lavoriamo"
  </behavior>

  <action>
Creare il file `content/services.ts` con questa struttura ESATTA. Header file e tipi per primi, poi la costante. Tutto in italiano, tono "serio ma non freddo, concreto, istituzionale" (D-11). Nessuna esclamazione, nessuna domanda retorica, nessun emoji, nessun "passion / innovation / cutting-edge".

```typescript
// content/services.ts
// Tutto il copy e i dati della pagina /servizi.
// Importato da app/servizi/page.tsx e passato come props alle sezioni.
// Nessun import esterno — puro TypeScript, no JSX, no React.

export type SoaBadge = {
  readonly code: string; // "OG1" | "OG2" | "OG3"
  readonly name: string; // "Edifici civili e industriali"
  readonly description: string; // 1-2 righe in italiano leggibile
};

export type ServicesTarget = {
  readonly id: string; // "privati" | "pubblico" | "professionisti"
  readonly n: string; // "01" | "02" | "03"
  readonly kicker: string; // eyebrow label — DEVE matchare homepageContent.services.items[N].kicker
  readonly title: string;
  readonly body: string;
  readonly imageSrc: string;
  readonly imageAlt: string;
  readonly tags: readonly string[];
  readonly ctaLabel: string;
  readonly ctaHref: string;
  readonly soaBadges?: readonly SoaBadge[]; // SOLO per target "pubblico"
};

export type ProcessStep = {
  readonly n: string; // "01" | "02" | "03" | "04"
  readonly title: string;
  readonly description: string;
};

export type ServicesContent = {
  readonly hero: {
    readonly eyebrow: string;
    readonly titleStart: string;
    readonly titleAccent: string;
    readonly titleEnd: string;
    readonly subtitle: string;
  };
  readonly targetIndex: {
    readonly targets: readonly ServicesTarget[];
  };
  readonly howWeWork: {
    readonly eyebrow: string;
    readonly titleStart: string;
    readonly titleAccent: string;
    readonly titleEnd: string;
    readonly steps: readonly ProcessStep[];
  };
  readonly finalCta: {
    readonly eyebrow: string;
    readonly headline: string;
    readonly body: string;
    readonly primaryCta: { readonly label: string; readonly href: string };
    readonly secondaryCta: { readonly label: string; readonly href: string };
  };
};

export const servicesContent: ServicesContent = {
  hero: {
    eyebrow: "I NOSTRI SERVIZI",
    titleStart: "Soluzioni edilizie ",
    titleAccent: "per ogni esigenza",
    titleEnd: ".",
    subtitle:
      "Dal cantiere residenziale al restauro pubblico, dal capannone industriale all'urbanizzazione. Operiamo per privati, enti pubblici e aziende nel Polesine e in tutto il Veneto.",
  },
  targetIndex: {
    targets: [
      // ---- PRIVATI (id: "privati") ----
      {
        id: "privati",
        n: "01",
        kicker: "Privati", // MUST match homepageContent.services.items[0].kicker exactly
        title: "Case che invecchiano bene.",
        body: "Costruiamo e ristrutturiamo abitazioni con progettazione antisismica, materiali certificati e un unico interlocutore dal preventivo alla consegna. Direzione lavori interna, niente catena di subappalti.",
        imageSrc: "/images/design/img-residenziale.webp",
        imageAlt: "Cantiere residenziale — villa di pregio",
        tags: [
          "Nuove costruzioni antisismica",
          "Ristrutturazioni di pregio",
          "Case passive NZEB",
          "Direzione lavori interna",
        ],
        ctaLabel: "Scopri i servizi",
        ctaHref: "/contatti",
      },
      // ---- ENTI PUBBLICI (id: "pubblico") ----
      {
        id: "pubblico",
        n: "02",
        kicker: "Settore Pubblico", // MUST match homepageContent.services.items[1].kicker exactly
        title: "Opere in cui la comunità investe.",
        body: "Affianchiamo Comuni, ASL ed enti del territorio nella realizzazione di opere pubbliche, scuole, strutture sanitarie e restauro conservativo. Esperienza consolidata nei rapporti con Soprintendenze e stazioni appaltanti.",
        imageSrc: "/images/design/img-pubblico.webp",
        imageAlt: "Cantiere pubblico — restauro istituzionale",
        tags: [
          "Appalti pubblici SOA",
          "Restauro conservativo",
          "Scuole e strutture sanitarie",
          "Urbanizzazioni",
        ],
        ctaLabel: "Scopri i servizi",
        ctaHref: "/contatti",
        soaBadges: [
          {
            code: "OG1",
            name: "Edifici civili e industriali",
            description:
              "Costruzione, ristrutturazione e ampliamento di edifici di qualsiasi natura.",
          },
          {
            code: "OG2",
            name: "Restauro e manutenzione di beni tutelati",
            description:
              "Interventi su edifici sottoposti a tutela ai sensi del Codice dei beni culturali.",
          },
          {
            code: "OG3",
            name: "Strade, autostrade, ponti",
            description:
              "Opere di urbanizzazione primaria, infrastrutture viarie e manufatti complementari.",
          },
        ],
      },
      // ---- PROFESSIONISTI / AZIENDE (id: "professionisti") ----
      {
        id: "professionisti",
        n: "03",
        kicker: "Aziende & Professionisti", // MUST match homepageContent.services.items[2].kicker exactly
        title: "Spazi che lavorano con te.",
        body: "Capannoni industriali, sedi direzionali, strutture commerciali. Tempistiche contrattuali, manutenzione programmata e cantieri che non fermano la tua attività. Collaboriamo con studi di architettura e ingegneria come general contractor di fiducia.",
        imageSrc: "/images/design/img-industriale.webp",
        imageAlt: "Cantiere industriale — capannone produttivo",
        tags: [
          "General contractor",
          "Capannoni e strutture industriali",
          "Sedi direzionali",
          "Manutenzione programmata",
        ],
        ctaLabel: "Scopri i servizi",
        ctaHref: "/contatti",
      },
    ],
  },
  howWeWork: {
    eyebrow: "Come lavoriamo",
    titleStart: "Quattro fasi, ",
    titleAccent: "un solo referente",
    titleEnd: ".",
    steps: [
      {
        n: "01",
        title: "Analisi",
        description:
          "Sopralluogo in cantiere, verifica della fattibilità, ascolto delle esigenze del committente.",
      },
      {
        n: "02",
        title: "Progettazione",
        description:
          "Definizione tecnica, computo metrico, cronoprogramma e preventivo a corpo dettagliato.",
      },
      {
        n: "03",
        title: "Realizzazione",
        description:
          "Maestranze dirette, direzione lavori interna, coordinamento sicurezza ISO 45001.",
      },
      {
        n: "04",
        title: "Consegna",
        description:
          "Collaudo, documentazione tecnica, garanzie e supporto post-consegna sul lavoro eseguito.",
      },
    ],
  },
  finalCta: {
    eyebrow: "Iniziamo a parlarne",
    headline: "Hai un progetto? Parliamone.",
    body: "Raccontaci l'opera. Veniamo sul posto, valutiamo fattibilità e tempi, e prepariamo un'offerta dettagliata entro dieci giorni lavorativi. Nessun costo, nessun impegno.",
    primaryCta: { label: "Richiedi un sopralluogo", href: "/contatti" },
    secondaryCta: { label: "Richiedi un preventivo", href: "/contatti" },
  },
} as const;
```

Note operative:

1. Le chiavi `kicker` DEVONO essere identiche stringa-per-stringa a `homepageContent.services.items[N].kicker` in `content/homepage.ts` (verificato: "Privati", "Settore Pubblico", "Aziende & Professionisti"). Questa è una regola di consistency cross-page (UI-SPEC Copywriting Contract).
2. La label primary CTA `"Richiedi un sopralluogo"` è bloccata dal CLAUDE.md (CTA Rules) e da UI-SPEC Copywriting Contract — NON modificare.
3. Le `tags` di ogni target devono coprire collettivamente i 4 servizi mandatori SRV-04: "Nuove costruzioni" (Privati), "Ristrutturazioni di pregio" (Privati), "Urbanizzazioni" (Enti Pubblici), "Appalti pubblici SOA" che incarna le opere pubbliche (Enti Pubblici). Il termine "opere pubbliche" appare in body Enti Pubblici.
4. Tutti gli `href` puntano a `/contatti` — non esistono pagine di dettaglio per-servizio (deferred — vedi 03-CONTEXT.md).
5. `as const` finale è obbligatorio per preservare la readonly inference profonda (stesso pattern di homepage.ts line 297).
   </action>

  <verify>
    <automated>pnpm typecheck</automated>
  </verify>

<acceptance_criteria> - Il file `content/services.ts` esiste - `pnpm typecheck` esce con codice 0 (nessun errore TypeScript) - `grep -E "^export type (SoaBadge|ServicesTarget|ProcessStep|ServicesContent)" content/services.ts` ritorna 4 righe - `grep -E "^export const servicesContent: ServicesContent" content/services.ts` ritorna 1 riga - `grep -E "} as const;" content/services.ts` ritorna almeno 1 riga (l'export finale) - `grep -c "Richiedi un sopralluogo" content/services.ts` ritorna almeno 1 - `grep -c "id: \"privati\"" content/services.ts` ritorna 1 - `grep -c "id: \"pubblico\"" content/services.ts` ritorna 1 - `grep -c "id: \"professionisti\"" content/services.ts` ritorna 1 - `grep -c "code: \"OG1\"" content/services.ts` ritorna 1 - `grep -c "code: \"OG2\"" content/services.ts` ritorna 1 - `grep -c "code: \"OG3\"" content/services.ts` ritorna 1 - `grep -c "kicker: \"Privati\"" content/services.ts` ritorna 1 - `grep -c "kicker: \"Settore Pubblico\"" content/services.ts` ritorna 1 - `grep -c "kicker: \"Aziende & Professionisti\"" content/services.ts` ritorna 1 - `grep -ci "urbanizzazion" content/services.ts` ritorna almeno 1 (copre SRV-04) - `grep -ci "ristrutturazion" content/services.ts` ritorna almeno 1 (copre SRV-04) - `grep -ci "nuove costruzion" content/services.ts` ritorna almeno 1 (copre SRV-04) - `grep -ci "opere pubbliche\|opere in cui la comunità\|appalti pubblici" content/services.ts` ritorna almeno 1 (copre SRV-04) - `grep -E "^import " content/services.ts` ritorna 0 righe (no imports — pure TS, regola da homepage.ts) - `grep -c "\"01\"" content/services.ts` ritorna almeno 4 (3 target n + 1 step n) - `pnpm check:compliance` esce con codice 0 (no GA4, no iframe, etc.)
</acceptance_criteria>

  <done>
    File `content/services.ts` creato, compila con TypeScript strict, esporta i 4 tipi e la costante `servicesContent`, contiene tutti gli id ancora ('privati', 'pubblico', 'professionisti'), i 3 SOA badges (OG1/OG2/OG3), i 4 step processo, e i 4 servizi SRV-04 (nuove costruzioni / ristrutturazioni / opere pubbliche / urbanizzazioni). Pronto per essere consumato dai componenti di Plan 02 e dall'orchestrator di Plan 03.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passa senza errori
- `pnpm check:compliance` passa
- Tutti gli id ancora ('privati', 'pubblico', 'professionisti') presenti
- Tutti i codici SOA (OG1, OG2, OG3) presenti come campo `code`
- Stringa CTA "Richiedi un sopralluogo" presente
- Cross-page kicker consistency: i kicker matchano homepage.ts esattamente
</verification>

<success_criteria>

- File `content/services.ts` esiste, type-safe, importabile
- Esporta 4 tipi: SoaBadge, ServicesTarget, ProcessStep, ServicesContent
- Esporta `servicesContent` come `as const`
- Copre i 4 servizi mandatori SRV-04 nei tags + body
- Implementa decisioni D-01 (hero), D-07 (servizi per target), D-08 (SOA badges), D-09 (4 step), D-11 (tone), D-12 (zero hardcoded strings)
  </success_criteria>

<output>
Dopo completion creare `.planning/phases/03-servizi/03-01-SUMMARY.md` con:
- Tipi esportati
- Numero target / step / SOA badges definiti
- Conferma cross-page kicker consistency
- Output di `pnpm typecheck`
</output>
