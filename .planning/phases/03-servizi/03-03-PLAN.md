---
phase: 03-servizi
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - app/servizi/page.tsx
autonomous: false
requirements:
  - SRV-01
  - SRV-02
  - SRV-03
  - SRV-04
  - SRV-05
must_haves:
  truths:
    - "Visitando `/servizi` l'utente vede in ordine: hero introduttivo, 3 card target, sezione Privati, sezione Enti Pubblici (con SOA badges OG1/OG2/OG3 visibili), sezione Professionisti, sezione 'Come lavoriamo' a 4 step, CTA finale 'Richiedi un sopralluogo'"
    - "Cliccando una delle 3 card target, la pagina scorre alla rispettiva sezione editoriale (anchor `/servizi#privati`, `#pubblico`, `#professionisti` funzionanti)"
    - "Il visitatore vede esplicitamente i codici SOA OG1, OG2, OG3 con descrizione testuale come elementi visivi distinti — non solo nel corpo testo"
    - "La CTA finale 'Richiedi un sopralluogo' è presente prima del footer (riusa HomepageCta esistente — zero modifiche)"
    - "La pagina supera `pnpm build` (build statica) e `pnpm check` (lint + typecheck + compliance + contrast + layout)"
  artifacts:
    - path: "app/servizi/page.tsx"
      provides: "Orchestratore RSC per /servizi — importa metadata, content, sezioni; nessuna logica business inline"
      contains: "ServicesHero"
      contains_also:
        - "TargetIndex"
        - "ServicesEditorialRow"
        - "HowWeWork"
        - "HomepageCta"
        - "buildMetadata"
        - "servicesContent"
        - 'alternates: { canonical: "/servizi" }'
  key_links:
    - from: "app/servizi/page.tsx"
      to: "content/services.ts (Plan 01) + 4 components from Plan 02 + components/sections/HomepageCta.tsx (Phase 2 reuse)"
      via: "named imports"
      pattern: 'from "@/components/sections/'
    - from: "TargetIndex card hrefs (`/servizi#${id}`)"
      to: "ServicesEditorialRow `<section id={id}>` rendered da page.tsx con id mappato da target.id"
      via: "browser native fragment scroll (html scroll-behavior: smooth)"
      pattern: "/servizi#"
---

<objective>
Sostituire lo stub `app/servizi/page.tsx` (creato in Phase 2 come placeholder "in arrivo") con l'orchestratore RSC reale che monta le sezioni di Phase 3 nell'ordine corretto e configura i metadata SEO della pagina.

Purpose: Tutte le decisioni precedenti (Plan 01 content + Plan 02 componenti) producono valore solo quando la pagina effettivamente li monta in un ordine che soddisfa il flow definito in 03-CONTEXT.md (D-01→D-18) e i success criteria della ROADMAP Phase 3. Questo è il punto di chiusura della fase.

Output: `app/servizi/page.tsx` orchestratore minimal che produce la pagina /servizi navigabile completa, più verifica end-to-end via build + checkpoint visivo umano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-servizi/03-CONTEXT.md
@.planning/phases/03-servizi/03-UI-SPEC.md
@.planning/phases/03-servizi/03-PATTERNS.md
@.planning/ROADMAP.md
@app/page.tsx
@app/servizi/page.tsx
@lib/seo/metadata.ts
@content/services.ts
@components/sections/HomepageCta.tsx

<interfaces>
<!-- Orchestrator pattern reference from app/page.tsx (lines 1-33) — copy structure, swap content + sections. -->

```typescript
// From app/page.tsx (verbatim) — the canonical orchestrator pattern.
import { buildMetadata } from "@/lib/seo/metadata";
import { homepageContent } from "@/content/homepage";
// ... section imports
import { HomepageCta } from "@/components/sections/HomepageCta";

export const metadata = buildMetadata({
  title: "Impresa Edile — Porto Viro, Rovigo, Veneto",
  description: siteContent.brand.tagline,
  alternates: { canonical: "/" },
});

export default function Home() {
  return (
    <>
      <HeroSection {...homepageContent.hero} />
      // ... etc
      <HomepageCta {...homepageContent.finalCta} />
    </>
  );
}
```

<!-- buildMetadata signature from lib/seo/metadata.ts (line 55) -->

```typescript
export function buildMetadata(overrides: Metadata = {}): Metadata;
// Title template "%s — Costruzioni Edilferro" applied automatically by defaultMetadata.
```

<!-- HomepageCta props (existing component, DO NOT modify — components/sections/HomepageCta.tsx lines 11-17) -->

```typescript
type HomepageCtaProps = {
  eyebrow: string;
  headline: string;
  body: string;
  primaryCta: { label: string; href: string };
  secondaryCta: { label: string; href: string };
};
```

<!-- Components imported from Plan 02 (their props shapes verified during Plan 02 acceptance) -->

```typescript
import { ServicesHero } from "@/components/sections/ServicesHero";
// Props: { eyebrow, titleStart, titleAccent, titleEnd, subtitle }

import { TargetIndex } from "@/components/sections/TargetIndex";
// Props: { targets: readonly ServicesTarget[] }

import { ServicesEditorialRow } from "@/components/sections/ServicesEditorialRow";
// Props: { id: string; variant: "panna" | "white"; item: ServicesTarget; reverse: boolean }

import { HowWeWork } from "@/components/sections/HowWeWork";
// Props: { eyebrow, titleStart, titleAccent, titleEnd, steps: readonly ProcessStep[] }
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace app/servizi/page.tsx stub with full orchestrator</name>
  <files>app/servizi/page.tsx</files>

<read_first> - app/servizi/page.tsx (current stub — will be entirely replaced) - app/page.tsx (entire file — canonical orchestrator pattern; copy structure exactly) - lib/seo/metadata.ts (entire file — confirm buildMetadata signature and metadataBase) - content/services.ts (consume `servicesContent.hero`, `servicesContent.targetIndex`, `servicesContent.howWeWork`, `servicesContent.finalCta`) - components/sections/HomepageCta.tsx (lines 11-17 — confirm props shape unchanged from Phase 2; reused as-is per D-14) - .planning/phases/03-servizi/03-PATTERNS.md ("app/servizi/page.tsx" section lines 465-521 — exact orchestrator JSX) - .planning/phases/03-servizi/03-UI-SPEC.md (full document — verify final flow matches Component Inventory + Cross-Cutting Consistency Checklist) - .planning/phases/03-servizi/03-CONTEXT.md (D-13 RSC default, D-14 HomepageCta reuse, D-18 alternating panna/white) - .planning/ROADMAP.md §"Phase 3: Servizi" Success Criteria (4 truths to satisfy)
</read_first>

  <behavior>
    `app/servizi/page.tsx`:
    - RSC (no `"use client"`)
    - Esporta `metadata` via `buildMetadata()` con title "Servizi", description focused, canonical "/servizi"
    - Esporta default function component `ServiziPage`
    - Importa: buildMetadata, servicesContent, ServicesHero, TargetIndex, ServicesEditorialRow, HowWeWork, HomepageCta
    - Renderizza in ordine: ServicesHero → TargetIndex → 3x ServicesEditorialRow (uno per target, alternando variant panna/white e reverse) → HowWeWork → HomepageCta
    - Le 3 ServicesEditorialRow sono generate via `.map()` su `servicesContent.targetIndex.targets`. Per ogni target:
      - `key={target.id}`
      - `id={target.id}` (questo diventa il `<section id="...">` su cui scorre il fragment link)
      - `item={target}`
      - `variant={i % 2 === 0 ? "panna" : "white"}` (i=0 panna, i=1 white, i=2 panna)
      - `reverse={i % 2 === 1}` (i=0 photo right, i=1 photo left, i=2 photo right)
    - Nessuna logica business, nessuna funzione locale, nessun stato — pure orchestrator
  </behavior>

  <action>
Sostituire INTEGRALMENTE il contenuto di `app/servizi/page.tsx` con questo file. Il vecchio stub "In arrivo" viene rimpiazzato.

```tsx
// app/servizi/page.tsx
// Pagina /servizi — orchestratore RSC. Importa sezioni e passa dati dal content module.
// Nessun hardcoding: tutto il copy viene da content/services.ts.
// HomepageCta riutilizzato as-is da Phase 2 per consistency cross-page (D-14).
import { buildMetadata } from "@/lib/seo/metadata";
import { servicesContent } from "@/content/services";
import { ServicesHero } from "@/components/sections/ServicesHero";
import { TargetIndex } from "@/components/sections/TargetIndex";
import { ServicesEditorialRow } from "@/components/sections/ServicesEditorialRow";
import { HowWeWork } from "@/components/sections/HowWeWork";
import { HomepageCta } from "@/components/sections/HomepageCta";

export const metadata = buildMetadata({
  title: "Servizi",
  description:
    "Soluzioni edilizie per privati, enti pubblici e aziende. Attestazione SOA OG1-OG2-OG3. Porto Viro, Rovigo, Veneto.",
  alternates: { canonical: "/servizi" },
});

export default function ServiziPage() {
  const { hero, targetIndex, howWeWork, finalCta } = servicesContent;
  return (
    <>
      <ServicesHero {...hero} />
      <TargetIndex targets={targetIndex.targets} />
      {targetIndex.targets.map((target, i) => (
        <ServicesEditorialRow
          key={target.id}
          id={target.id}
          item={target}
          variant={i % 2 === 0 ? "panna" : "white"}
          reverse={i % 2 === 1}
        />
      ))}
      <HowWeWork {...howWeWork} />
      <HomepageCta {...finalCta} />
    </>
  );
}
```

Note tecniche:

- Posizione di HowWeWork: tra le sezioni editoriali e il finalCta (decisione planner — UI-SPEC §"Come lavoriamo" line 186 lock-in `bg-white` "to break alternation rhythm and signal 'process moment'"; coerente con flow narrativo: prima i 3 target, poi il processo che li accomuna, poi la CTA).
- Sequenza variant/reverse generata da `i`:
  - i=0 (Privati): variant="panna", reverse=false → photo a destra, sfondo panna
  - i=1 (Enti Pubblici): variant="white", reverse=true → photo a sinistra, sfondo bianco; SOA badges visibili (passate via `item.soaBadges` automaticamente)
  - i=2 (Professionisti): variant="panna", reverse=false → photo a destra, sfondo panna
- Il pattern `.map()` non passa `soaBadges` come prop separato — `ServicesEditorialRow` lo prende da `item.soaBadges` (il prop interface accetta solo `id, variant, item, reverse`).
- Description metadata sopra include "OG1-OG2-OG3" e "Porto Viro, Rovigo, Veneto" — supporta SEO long-tail e differenziazione SOA.
- Nessun JSON-LD aggiuntivo qui — Phase 7 si occuperà di GeneralContractor JSON-LD globale.
- HomepageCta riusato as-is — il finalCta in `content/services.ts` ha lo stesso shape di `homepageContent.finalCta`.
  </action>

    <verify>
      <automated>pnpm check</automated>
    </verify>

  <acceptance_criteria> - File `app/servizi/page.tsx` esiste e NON contiene più "In arrivo" o "stub" (vecchio contenuto sostituito) - `pnpm typecheck` esce con codice 0 - `pnpm lint` esce con codice 0 - `pnpm check:compliance` esce con codice 0 - `pnpm check:contrast` esce con codice 0 - `pnpm check:layout` esce con codice 0 - `pnpm build` esce con codice 0 (build statica completa) - `grep -c "use client" app/servizi/page.tsx` ritorna 0 (RSC) - `grep -c "import { buildMetadata }" app/servizi/page.tsx` ritorna 1 - `grep -c "import { servicesContent }" app/servizi/page.tsx` ritorna 1 - `grep -c "import { ServicesHero }" app/servizi/page.tsx` ritorna 1 - `grep -c "import { TargetIndex }" app/servizi/page.tsx` ritorna 1 - `grep -c "import { ServicesEditorialRow }" app/servizi/page.tsx` ritorna 1 - `grep -c "import { HowWeWork }" app/servizi/page.tsx` ritorna 1 - `grep -c "import { HomepageCta }" app/servizi/page.tsx` ritorna 1 - `grep -c "alternates: { canonical: \"/servizi\" }" app/servizi/page.tsx` ritorna 1 - `grep -c "OG1-OG2-OG3\|OG1, OG2, OG3" app/servizi/page.tsx` ritorna at least 1 (description metadata SOA mention) - `grep -c ".map((target, i)" app/servizi/page.tsx` ritorna 1 - `grep -c "id={target.id}" app/servizi/page.tsx` ritorna 1 - `grep -c "variant={i % 2 === 0 ? \"panna\" : \"white\"}" app/servizi/page.tsx` ritorna 1 - `grep -c "reverse={i % 2 === 1}" app/servizi/page.tsx` ritorna 1 - `grep -c "In arrivo" app/servizi/page.tsx` ritorna 0 (vecchio stub eliminato) - `grep -c "export default function ServiziPage" app/servizi/page.tsx` ritorna 1
  </acceptance_criteria>

    <done>
      `app/servizi/page.tsx` sostituito completamente. Esporta metadata + default ServiziPage. Renderizza in ordine: ServicesHero → TargetIndex → 3x ServicesEditorialRow (variant alternato + reverse alternato + SOA badges automaticamente per Enti Pubblici) → HowWeWork → HomepageCta. `pnpm check` (lint + typecheck + compliance + contrast + layout) e `pnpm build` passano.
    </done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — visual + interaction sanity check on /servizi</name>
  <files>app/servizi/page.tsx, components/sections/ServicesHero.tsx, components/sections/TargetIndex.tsx, components/sections/ServicesEditorialRow.tsx, components/sections/HowWeWork.tsx, content/services.ts</files>

<read_first> - .planning/ROADMAP.md §"Phase 3: Servizi" Success Criteria (the 4 truths to verify) - .planning/phases/03-servizi/03-UI-SPEC.md §"Cross-Cutting Consistency Checklist"
</read_first>

  <what-built>
    L'esecutore ha creato:
    1. `content/services.ts` — content module con tutto il copy della pagina
    2. 4 nuovi componenti in `components/sections/` (ServicesHero, TargetIndex, ServicesEditorialRow, HowWeWork)
    3. `app/servizi/page.tsx` rimpiazzato con orchestratore reale
    4. Build statica completa (`pnpm build`) passa
    5. Tutti i check automatici (`pnpm check`) passano
  </what-built>

  <action>
PAUSA per verifica umana. Non eseguire altre modifiche di codice. L'utente deve seguire i passi in `<how-to-verify>` qui sotto e rispondere "approved" o descrivere problemi specifici. Dopo la risposta dell'utente:
- Se "approved" → completare il SUMMARY del plan e marcare la fase Phase 3 come ready-to-close
- Se l'utente segnala problemi → rientrare in revision mode con fix mirati ai componenti coinvolti, NON ripianificare l'intera fase
  </action>

  <how-to-verify>
    Avvia il dev server e verifica visivamente i seguenti punti — questi sono i 4 success criteria di Phase 3 dalla ROADMAP, più la consistency checklist UI-SPEC:

    **Step 1 — Avvio dev server**
    ```bash
    pnpm dev
    ```
    Apri il browser su `http://localhost:3000/servizi`.

    **Step 2 — Verifica anchor navigation (SRV-01 — ROADMAP success criterion #1)**
    1. La pagina deve mostrare in ordine: hero centrato → 3 card target → sezione Privati → sezione Enti Pubblici → sezione Professionisti → sezione "Come lavoriamo" → CTA finale
    2. Click sulla card "Privati" → la pagina deve scorrere fino alla sezione Privati (verifica nell'URL appare `#privati`)
    3. Apri direttamente `http://localhost:3000/servizi#pubblico` in una nuova tab → la pagina deve aprirsi già scrollata alla sezione Enti Pubblici
    4. Apri direttamente `http://localhost:3000/servizi#professionisti` → idem per Professionisti

    **Step 3 — Verifica SOA esplicito (SRV-03 — ROADMAP success criterion #2)**
    1. Scrolla alla sezione Enti Pubblici
    2. Sotto la lista bullet dei servizi devono apparire 3 CARD distinte con rounded-lg, ognuna contenente:
       - Codice in alto (OG1, OG2, OG3) in text-brand uppercase Neue Montreal
       - Nome categoria sotto in serif (es. "Edifici civili e industriali")
       - Descrizione 1-2 righe in text-ink/70
    3. Le badge devono essere visivamente DISTINTE dai bullet pill (forma diversa: rectangular vs pill), non confondibili

    **Step 4 — Verifica copertura 4 servizi (SRV-04 — ROADMAP success criterion #3)**
    Verifica che nei testi delle 3 sezioni target compaiano TUTTI e 4 i servizi mandatori:
    - "nuove costruzioni" (Privati — bullet o body)
    - "ristrutturazioni" (Privati — bullet o body)
    - "opere pubbliche" o "appalti pubblici" (Enti Pubblici — body)
    - "urbanizzazioni" (Enti Pubblici — bullet o body)

    **Step 5 — Verifica CTA finale (SRV-05 — ROADMAP success criterion #4)**
    1. Scrolla in fondo: deve essere visibile la sezione `<HomepageCta>` con sfondo blu brand
    2. Il pulsante primario deve dire ESATTAMENTE "Richiedi un sopralluogo"
    3. Cliccando il pulsante porta a `/contatti`

    **Step 6 — Verifica responsive mobile (CLAUDE.md mobile-first)**
    1. Apri DevTools → Toggle device toolbar → iPhone SE (375px width)
    2. Niente scroll orizzontale visibile in nessuna sezione
    3. Le 3 card target stack verticali (D-02)
    4. Le sezioni editoriali stack text-above-photo (D-04)
    5. I 4 step "Come lavoriamo" stack verticali (D-10)

    **Step 7 — Verifica visual consistency con homepage (D-03)**
    1. Apri `http://localhost:3000` (homepage) e `http://localhost:3000/servizi` in due tab
    2. Eyebrow style identico (tracking, spacing, hairline)
    3. Bullet pill style identico (forma rounded-full, padding, border)
    4. Arrow CTA con `→` identico (mai usare Lucide ArrowRight)
    5. Header e footer pixel-identici tra le due pagine

    **Step 8 — Hover behavior (D-03)**
    1. Hover su una card target → border si scurisce, freccia trasla a destra (no scale, no shadow)
    2. Hover su un bullet pill → border passa a brand/50
    3. Hover sull'immagine di una sezione editoriale → leggero zoom 1.04

    **Step 9 — Build production sanity**
    ```bash
    pnpm build
    ```
    Output deve mostrare la route `/servizi` come Static (○) — NOT Dynamic (ƒ).

    **Se vedi qualcosa che non torna:**
    - Sezione che non scrolla all'ancora → verifica `id={id}` su `<section>` in ServicesEditorialRow
    - SOA badge non visibili → verifica che i 3 codici OG siano nel content + che il rendering condizionale `item.soaBadges && ...` funzioni
    - Card non cliccabili o cliccabili solo sul testo → verifica che `<Link>` wrappi TUTTO il contenuto della card
    - Tipografia incoerente con homepage → verifica clamp values e tracking-tight nei nuovi componenti

  </how-to-verify>

  <verify>
    <automated>echo "Manual verification step — see how-to-verify"</automated>
  </verify>

<acceptance_criteria> - L'utente ha eseguito `pnpm dev` e visitato `/servizi` - L'utente conferma che i 9 step di verifica passano - L'utente risponde "approved" oppure ha identificato problemi specifici (sezione + comportamento atteso vs osservato) - Se "approved": tutti e 4 i success criteria della Phase 3 ROADMAP sono verificati visivamente
</acceptance_criteria>

  <done>
    Utente ha digitato "approved" oppure ha fornito feedback puntuale che entra in revision queue. Phase 3 è verificata end-to-end o ha una lista chiara di fix.
  </done>

<resume-signal>Type "approved" se tutto torna, oppure descrivi precisamente cosa non va (sezione + comportamento atteso vs osservato).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

`/servizi` è una pagina 100% statica RSC. Le uniche superfici di interazione sono link interni (`<Link>`) con href known-at-compile-time. Nessun input utente, nessun fetch da API esterne, nessun parametro URL parsato (gli anchor fragment sono gestiti dal browser nativamente). Nessun trust boundary rilevante per questo phase.

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                                                                                                              |
| --------- | ---------------------- | -------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-03   | Information disclosure | metadata description | accept      | Description contiene solo informazioni pubbliche già esposte sulla homepage e in JSON-LD finale (Phase 7). Nessun PII.                                                                       |
| T-03-04   | Spoofing               | Anchor fragments     | accept      | Fragment `#privati` etc. sono client-side only, non instrumentano alcuna logica server. Anchor invalida silently scrolls to top (UI-SPEC §Copywriting Contract — accepted browser behavior). |

</threat_model>

<verification>
- `pnpm check` (lint + typecheck + compliance + contrast + layout) passa
- `pnpm build` completa senza errori
- `/servizi` renderizza tutte le sezioni in ordine
- Anchor scroll funziona (SRV-01)
- SOA badges visibili come elementi distinti (SRV-03)
- Tutti i 4 servizi mandatori menzionati (SRV-04)
- CTA finale "Richiedi un sopralluogo" presente (SRV-05)
- Visual parity con homepage verificata umanamente
</verification>

<success_criteria>

- ROADMAP Phase 3 success criterion #1: `/servizi` navigabile via anchor link diretti — VERIFIED
- ROADMAP Phase 3 success criterion #2: Sezione Enti Pubblici nomina esplicitamente le categorie SOA — VERIFIED
- ROADMAP Phase 3 success criterion #3: Tutti e 4 i servizi principali hanno una sezione/menzione dedicata — VERIFIED
- ROADMAP Phase 3 success criterion #4: CTA "Richiedi un sopralluogo" visibile a fine pagina — VERIFIED
- Build statica + check passano
- Visual consistency con homepage approvata da utente
  </success_criteria>

<output>
Dopo completion creare `.planning/phases/03-servizi/03-03-SUMMARY.md` con:
- Conferma sostituzione stub `app/servizi/page.tsx`
- Output di `pnpm check` + `pnpm build`
- Lista delle 5 sezioni renderizzate in ordine
- Conferma checkpoint umano approvato (testo "approved" o note dell'utente)
- Status finale dei 5 requirement IDs (SRV-01 → SRV-05): tutti coperti
</output>
