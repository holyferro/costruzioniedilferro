---
phase: 03-servizi
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - components/sections/ServicesHero.tsx
  - components/sections/TargetIndex.tsx
  - components/sections/ServicesEditorialRow.tsx
  - components/sections/HowWeWork.tsx
autonomous: true
requirements:
  - SRV-01
  - SRV-02
  - SRV-03
  - SRV-04
must_haves:
  truths:
    - "I 4 nuovi componenti sezione esistono in components/sections/ e compilano TypeScript strict"
    - 'ServicesEditorialRow accetta `id` prop che diventa `id="..."` sul `<section>` (anchor target per link `/servizi#privati`)'
    - "ServicesEditorialRow renderizza SOA badges quando il prop `soaBadges` è presente, altrimenti li omette"
    - "TargetIndex renderizza 3 card-link wrappate in `<Link>` (fully clickable per D-02)"
    - "HowWeWork renderizza 4 step con icone Lucide (ClipboardList, Ruler, HardHat, CheckCircle2)"
    - 'Tutti i componenti sono RSC — zero `"use client"`'
    - "Tutti i componenti rispettano la regola fill-only del blu brand (Phase 1 D-06): blu solo come background CTA, accent italic H2, numero serif italic, icon stroke fra le eccezioni autorizzate dall'UI-SPEC §Color"
  artifacts:
    - path: "components/sections/ServicesHero.tsx"
      provides: "Hero centrato testuale su bg-panna con H1 serif clamp + Inter subtitle"
      exports: ["ServicesHero"]
      contains: "bg-panna"
    - path: "components/sections/TargetIndex.tsx"
      provides: "3-card-link grid bg-panna che porta agli ancori #privati / #pubblico / #professionisti"
      exports: ["TargetIndex"]
      contains: "md:grid-cols-3"
    - path: "components/sections/ServicesEditorialRow.tsx"
      provides: "Row 2-col text+photo con id ancora, variant panna|white, slot SOA badges opzionale"
      exports: ["ServicesEditorialRow"]
      contains: "id={id}"
    - path: "components/sections/HowWeWork.tsx"
      provides: "4-step grid con Lucide icons su bg-white"
      exports: ["HowWeWork"]
      contains: "lucide-react"
  key_links:
    - from: "TargetIndex card click"
      to: 'ServicesEditorialRow `<section id="...">`'
      via: "Next.js Link href con fragment (`/servizi#privati` etc.) + native browser scroll-behavior smooth"
      pattern: 'href="/servizi#'
    - from: "All 4 components"
      to: "content/services.ts types"
      via: 'import type { ServicesTarget, ProcessStep, SoaBadge } from "@/content/services"'
      pattern: 'from "@/content/services"'
---

<objective>
Creare 4 nuovi componenti di sezione in `components/sections/` che renderizzano la pagina `/servizi`: hero introduttivo, indice target a 3 card, righe editoriali ancorabili (con slot SOA per Enti Pubblici), e sezione "Come lavoriamo" a 4 step con icone Lucide.

Purpose: Phase 3 estende il visual language di Phase 2 (ServiceOverview / Values / FeaturedProjects) su superficie panna/white. I componenti riusano i pattern editoriali esistenti — eyebrow, kicker italic numerato, bullet pills, arrow CTA — e aggiungono un solo elemento nuovo (le SOA badge card) come differenziatore esplicito di SRV-03.

Output: 4 file `.tsx` in `components/sections/`, tutti RSC, tutti type-safe, pronti per essere orchestrati da `app/servizi/page.tsx` (Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-servizi/03-CONTEXT.md
@.planning/phases/03-servizi/03-UI-SPEC.md
@.planning/phases/03-servizi/03-PATTERNS.md
@components/sections/ServiceOverview.tsx
@components/sections/Values.tsx
@components/sections/FeaturedProjects.tsx
@content/services.ts
@app/globals.css
@CLAUDE.md

<interfaces>
<!-- Types consumed (created in Plan 01 — content/services.ts) -->

```typescript
// From content/services.ts
export type SoaBadge = {
  readonly code: string;
  readonly name: string;
  readonly description: string;
};

export type ServicesTarget = {
  readonly id: string;
  readonly n: string;
  readonly kicker: string;
  readonly title: string;
  readonly body: string;
  readonly imageSrc: string;
  readonly imageAlt: string;
  readonly tags: readonly string[];
  readonly ctaLabel: string;
  readonly ctaHref: string;
  readonly soaBadges?: readonly SoaBadge[];
};

export type ProcessStep = {
  readonly n: string;
  readonly title: string;
  readonly description: string;
};
```

<!-- Existing tokens locked from Phase 1 (app/globals.css) — DO NOT redefine -->

```css
--color-ink: #1a1a1a; /* text-ink, bg-ink */
--color-panna: #f8f5ee; /* text-panna, bg-panna */
--color-brand: #291572; /* text-brand, bg-brand — FILL ONLY per D-06 */
--color-border: #e6e1d6; /* border-border */
--color-surface: #ffffff; /* bg-surface */
--text-h1: clamp(2.25rem, 1.5rem + 3.2vw, 4rem); /* text-h1 utility */
```

<!-- Existing pattern from Phase 2 ServiceOverview ServiceRow (the analog for ServicesEditorialRow) -->

`components/sections/ServiceOverview.tsx` lines 62-138 — internal `ServiceRow` function: 2-col grid text+photo with alternating order, kicker row with italic serif "— NN" + uppercase eyebrow, H3 serif clamp, Inter body, bullet pills, inline arrow CTA, image link with hover scale + bottom gradient + huge serif "NN" overlay top-left.

<!-- Existing CSS — already set in app/globals.css (line 57) -->

`html { scroll-behavior: smooth; }` — native fragment scroll. NO client-side scroll handler needed for anchor links.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create ServicesHero.tsx + TargetIndex.tsx</name>
  <files>components/sections/ServicesHero.tsx, components/sections/TargetIndex.tsx</files>

<read_first> - components/sections/Values.tsx (entire file — analog for ServicesHero header pattern with light surface Eyebrow function) - components/sections/ServiceOverview.tsx (entire file — analog for TargetIndex card hover/cta + numbered eyebrow + bullet pill + arrow translate patterns) - components/sections/FeaturedProjects.tsx (lines 64-108 FeatureCard — full-card `<Link>` wrapper pattern reference) - .planning/phases/03-servizi/03-PATTERNS.md (sections "ServicesHero" lines 24-83 and "TargetIndex" lines 87-162 — full prescribed JSX) - .planning/phases/03-servizi/03-UI-SPEC.md ("Component Inventory" lines 152-156 + "Target Index Card Specification" lines 164-176) - content/services.ts (consume `ServicesTarget` type and `servicesContent.hero` shape)
</read_first>

  <behavior>
    `ServicesHero`:
    - RSC, no `"use client"`
    - Renderizza `<section className="bg-panna text-ink py-24 md:py-32">` con container `mx-auto max-w-6xl px-6 md:px-12`
    - Layout centrato: `text-center max-w-3xl mx-auto`
    - Renderizza Eyebrow (light surface, `text-ink/60` + hairline `bg-ink/40`)
    - H1 serif con clamp `text-[clamp(2.25rem,1.5rem+3.2vw,4rem)]`, `font-serif font-medium tracking-tight leading-[1.1]`
    - H1 contiene `<em className="text-brand font-serif italic">{titleAccent}</em>` per la parola accent
    - Subtitle Inter `text-base leading-relaxed text-ink/70 mt-7 max-w-[52ch] mx-auto`

    `TargetIndex`:
    - RSC, no `"use client"`
    - Renderizza `<section className="bg-panna text-ink py-16 md:py-20">` con container standard
    - Grid `grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-12 items-stretch`
    - 3 card, ognuna `<Link>` wrappante TUTTO il contenuto della card (fully clickable per D-02)
    - Ogni card: `bg-surface border border-border p-8 md:p-10 h-full transition-colors duration-300 hover:border-ink/40 focus-visible:outline-2 focus-visible:outline-brand focus-visible:outline-offset-4`
    - href della card = `/servizi#${target.id}` (es. `/servizi#privati`)
    - Inner: numbered eyebrow `— NN` italic serif text-brand + UPPERCASE kicker text-ink/60 → H3 serif text-ink → Inter subtitle (body) → arrow CTA con `group-hover:translate-x-1.5`
    - Nessuna scale/zoom sulla card (UI-SPEC §Target Index Card Specification — feedback only border-color + arrow translate)

  </behavior>

  <action>
**File 1: `components/sections/ServicesHero.tsx`**

```tsx
// components/sections/ServicesHero.tsx
// RSC. Hero introduttivo pagina /servizi: testuale, centrato, su bg-panna.
// Distinto dall'HeroSection homepage (che ha sfondo dark + foto fill).

type ServicesHeroProps = {
  eyebrow: string;
  titleStart: string;
  titleAccent: string;
  titleEnd: string;
  subtitle: string;
};

export function ServicesHero({
  eyebrow,
  titleStart,
  titleAccent,
  titleEnd,
  subtitle,
}: ServicesHeroProps) {
  return (
    <section className="bg-panna text-ink py-24 md:py-32">
      <div className="mx-auto max-w-6xl px-6 md:px-12">
        <div className="mx-auto max-w-3xl text-center">
          <Eyebrow>{eyebrow}</Eyebrow>
          <h1 className="text-ink mt-5 font-serif text-[clamp(2.25rem,1.5rem+3.2vw,4rem)] leading-[1.1] font-medium tracking-tight">
            {titleStart}
            <em className="text-brand font-serif italic">{titleAccent}</em>
            {titleEnd}
          </h1>
          <p className="text-ink/70 mx-auto mt-7 max-w-[52ch] text-base leading-relaxed md:text-lg">
            {subtitle}
          </p>
        </div>
      </div>
    </section>
  );
}

function Eyebrow({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-ink/60 text-xs font-semibold tracking-[0.38em] uppercase">
      <span aria-hidden="true" className="bg-ink/40 mr-3 inline-block h-px w-8 align-middle" />
      {children}
    </p>
  );
}
```

**File 2: `components/sections/TargetIndex.tsx`**

```tsx
// components/sections/TargetIndex.tsx
// RSC. Indice visivo a 3 card cliccabili (Privati / Enti Pubblici / Professionisti).
// Ogni card è un <Link> intero — la card è il click target (D-02).
// Link punta all'ancora #privati / #pubblico / #professionisti.

import Link from "next/link";
import type { Route } from "next";
import type { ServicesTarget } from "@/content/services";

type TargetIndexProps = {
  targets: readonly ServicesTarget[];
};

export function TargetIndex({ targets }: TargetIndexProps) {
  return (
    <section className="bg-panna text-ink py-16 md:py-20">
      <div className="mx-auto max-w-6xl px-6 md:px-12">
        <div className="grid grid-cols-1 items-stretch gap-6 md:grid-cols-3 md:gap-12">
          {targets.map((t) => (
            <TargetCard key={t.id} target={t} />
          ))}
        </div>
      </div>
    </section>
  );
}

function TargetCard({ target }: { target: ServicesTarget }) {
  const href = `/servizi#${target.id}` as Route<string>;
  return (
    <Link
      href={href}
      className="group bg-surface border-border hover:border-ink/40 focus-visible:outline-brand block h-full border p-8 transition-colors duration-300 focus-visible:outline-2 focus-visible:outline-offset-4 md:p-10"
    >
      {/* Numbered eyebrow row */}
      <div className="mb-4 flex items-baseline gap-4">
        <span className="text-brand font-serif text-sm font-medium italic">— {target.n}</span>
        <span className="text-ink/60 text-[11px] font-semibold tracking-[0.22em] uppercase">
          {target.kicker}
        </span>
      </div>

      {/* H3 serif card title */}
      <h3 className="text-ink font-serif text-[clamp(1.3rem,0.6rem+1vw,1.65rem)] leading-[1.25] font-medium tracking-tight">
        {target.title}
      </h3>

      {/* Inter subtitle (body shortened to first sentence visually) */}
      <p className="text-ink/70 mt-4 text-base leading-[1.6]">{target.body}</p>

      {/* Arrow CTA — group-hover translate (matches ServiceOverview group/cta pattern) */}
      <span className="text-ink border-ink/35 group-hover:border-ink mt-9 inline-flex items-center gap-2.5 border-b pb-1.5 font-[family-name:var(--font-neue-montreal)] text-xs tracking-[0.08em] uppercase transition-colors">
        {target.ctaLabel}
        <span
          aria-hidden="true"
          className="inline-block transition-transform duration-300 group-hover:translate-x-1.5"
        >
          →
        </span>
      </span>
    </Link>
  );
}
```

Note tecniche:

- `ServicesHero` non importa nulla (tipi inline, niente Image, niente Link, niente content) — è un componente pure presentational.
- `TargetIndex` importa il tipo `ServicesTarget` da `@/content/services` (creato in Plan 01).
- Il `<Link>` wrapper TargetCard usa `Route<string>` per supportare typedRoutes (riabilitato in Plan 02 di Phase 2).
- L'href è dinamico (`/servizi#${target.id}`) e Route<string> cast garantisce la compilazione type-checked.
- `group` (non `group/img` o `group/cta`) sulla `<Link>` — l'arrow translate è scoped al singolo card link.
- Nessun `Image` nel TargetCard (UI-SPEC: cards sono testuali, niente foto in card).
- Niente `bullet pills` mini-list nel card (UI-SPEC menziona "compact 3-4 item bullet list" come opzione, ma il body Inter copre il messaggio — evitiamo ridondanza con la sezione editoriale sotto, come segnalato in 03-CONTEXT.md §Specifics).
- `bg-surface` rende la card bianca su sezione panna — lift visivo senza shadow (UI-SPEC §Target Index Card).
- `rounded-none` di default (nessun `rounded-*` utility applicato — squared editorial language).
  </action>

    <verify>
      <automated>pnpm typecheck && pnpm lint</automated>
    </verify>

  <acceptance_criteria> - File `components/sections/ServicesHero.tsx` esiste - File `components/sections/TargetIndex.tsx` esiste - `pnpm typecheck` esce con codice 0 - `pnpm lint` esce con codice 0 - `grep -E "^export function ServicesHero" components/sections/ServicesHero.tsx` ritorna 1 riga - `grep -E "^export function TargetIndex" components/sections/TargetIndex.tsx` ritorna 1 riga - `grep -c "use client" components/sections/ServicesHero.tsx` ritorna 0 (RSC) - `grep -c "use client" components/sections/TargetIndex.tsx` ritorna 0 (RSC) - `grep -c "bg-panna" components/sections/ServicesHero.tsx` ritorna almeno 1 - `grep -c "py-24 md:py-32" components/sections/ServicesHero.tsx` ritorna 1 - `grep -c "clamp(2.25rem,1.5rem+3.2vw,4rem)" components/sections/ServicesHero.tsx` ritorna 1 - `grep -c "text-brand font-serif italic" components/sections/ServicesHero.tsx` ritorna 1 - `grep -c "md:grid-cols-3" components/sections/TargetIndex.tsx` ritorna 1 - `grep -c "items-stretch" components/sections/TargetIndex.tsx` ritorna 1 - `grep -c "h-full" components/sections/TargetIndex.tsx` ritorna 1 - `grep -c "/servizi#" components/sections/TargetIndex.tsx` ritorna 1 - `grep -c "focus-visible:outline-brand" components/sections/TargetIndex.tsx` ritorna 1 - `grep -c "group-hover:translate-x-1.5" components/sections/TargetIndex.tsx` ritorna 1 - `grep -c "from \"@/content/services\"" components/sections/TargetIndex.tsx` ritorna 1 - Nessun uso di `shadow-` in entrambi i file: `grep -c "shadow-" components/sections/ServicesHero.tsx components/sections/TargetIndex.tsx` ritorna 0 - Nessun uso di `rounded-xl` o `rounded-2xl` (institutional language): `grep -E "rounded-(xl|2xl|3xl)" components/sections/ServicesHero.tsx components/sections/TargetIndex.tsx` ritorna 0 righe
  </acceptance_criteria>

    <done>
      Entrambi i componenti esistono, RSC-only, type-checked, lint-clean. ServicesHero renderizza H1 serif clamp con accent brand italic su bg-panna centrato. TargetIndex renderizza 3 card-link cliccabili wrappanti tutto il contenuto, ognuna con href=/servizi#${id}, hover border + arrow translate, focus-visible outline brand.
    </done>
  </task>

<task type="auto" tdd="false">
  <name>Task 2: Create ServicesEditorialRow.tsx (with SOA badges slot)</name>
  <files>components/sections/ServicesEditorialRow.tsx</files>

<read_first> - components/sections/ServiceOverview.tsx (entire file — direct analog: copy ServiceRow pattern lines 62-138, then port to light surface tokens per PATTERNS table) - .planning/phases/03-servizi/03-PATTERNS.md ("ServicesEditorialRow" section lines 166-291 — exact JSX, color token swap table, SOA badge slot pattern, props shape) - .planning/phases/03-servizi/03-UI-SPEC.md ("Component Inventory" line 155, "SOA Badge Specification" lines 130-143, "Color" §Accent reserved for line 113-117) - .planning/phases/03-servizi/03-CONTEXT.md (D-04, D-08 SOA critical differentiator, D-15 image source, D-16 next/image with fill+sizes, D-18 alternating panna/white) - content/services.ts (consume `ServicesTarget` and `SoaBadge` types)
</read_first>

  <behavior>
    `ServicesEditorialRow`:
    - RSC, no `"use client"`
    - Renderizza `<section id={id} className={...}>` dove `id` è "privati" | "pubblico" | "professionisti" — questo è il TARGET ANCHOR per i link `/servizi#privati` etc.
    - Variant: `"panna"` → `bg-panna text-ink`, `"white"` → `bg-white text-ink`. Padding: `py-20 md:py-28`.
    - Layout 2-col text+photo alternato (`md:grid-cols-2`). `reverse=true` mette photo a sinistra, text a destra.
    - Section text contiene: numbered eyebrow row (`— NN` italic serif text-brand + UPPERCASE kicker text-ink/60), H2 serif clamp, body Inter, bullet pills (max-w container), optional SOA badges slot, inline arrow CTA.
    - Photo column è un `<Link>` separato (NON un wrapper sulla card intera) wrappante `<Image>` con fill + sizes + scale-on-hover + bottom gradient + serif overlay "NN" top-left.
    - SOA badges renderizzate solo se prop `soaBadges` è array non vuoto. 3-col grid desktop, stack mobile.
  </behavior>

  <action>
**File: `components/sections/ServicesEditorialRow.tsx`**

```tsx
// components/sections/ServicesEditorialRow.tsx
// RSC. Riga editoriale 2-col testo+foto per ogni target (Privati / Enti Pubblici / Professionisti).
// Pattern derivato da ServiceOverview ServiceRow (Phase 2) ma su superficie panna/bianca.
// Aggiunge slot SOA badges per la sezione Enti Pubblici (D-08 — differenziatore critico SRV-03).
// L'attributo `id` sul <section> è il target degli ancori da TargetIndex (`/servizi#privati` etc.).

import Image from "next/image";
import Link from "next/link";
import type { Route } from "next";
import type { ServicesTarget, SoaBadge } from "@/content/services";

type ServicesEditorialRowProps = {
  id: string; // "privati" | "pubblico" | "professionisti" — anchor target
  variant: "panna" | "white";
  item: ServicesTarget;
  reverse: boolean;
};

export function ServicesEditorialRow({ id, variant, item, reverse }: ServicesEditorialRowProps) {
  const surfaceClass = variant === "panna" ? "bg-panna" : "bg-white";
  return (
    <section id={id} className={`${surfaceClass} text-ink py-20 md:py-28`}>
      <div className="mx-auto max-w-6xl px-6 md:px-12">
        <div className="grid items-center gap-10 md:grid-cols-2 md:gap-16">
          {/* Text column */}
          <div className={reverse ? "md:order-2" : "md:order-1"}>
            <div className="mb-4 flex items-baseline gap-4">
              <span className="text-brand font-serif text-sm font-medium italic">— {item.n}</span>
              <span className="text-ink/60 text-[11px] font-semibold tracking-[0.22em] uppercase">
                {item.kicker}
              </span>
            </div>

            <h2 className="text-ink font-serif text-[clamp(1.75rem,0.8rem+2vw,2.6rem)] leading-[1.15] font-medium tracking-tight">
              {item.title}
            </h2>

            <p className="text-ink/80 mt-5 max-w-[52ch] text-base leading-[1.65]">{item.body}</p>

            <ul className="mt-7 flex flex-wrap gap-2">
              {item.tags.map((t) => (
                <li
                  key={t}
                  className="text-ink/85 border-ink/20 hover:border-brand/50 rounded-full border px-3.5 py-1.5 text-xs transition-colors duration-300"
                >
                  {t}
                </li>
              ))}
            </ul>

            {item.soaBadges && item.soaBadges.length > 0 ? (
              <SoaBadgeGrid badges={item.soaBadges} surface={variant} />
            ) : null}

            <Link
              href={item.ctaHref as Route<string>}
              className="text-ink border-ink/35 hover:border-ink group/cta mt-9 inline-flex items-center gap-2.5 border-b pb-1.5 font-[family-name:var(--font-neue-montreal)] text-xs tracking-[0.08em] uppercase transition-colors"
            >
              {item.ctaLabel}
              <span
                aria-hidden="true"
                className="inline-block transition-transform duration-300 group-hover/cta:translate-x-1.5"
              >
                →
              </span>
            </Link>
          </div>

          {/* Photo column */}
          <Link
            href={item.ctaHref as Route<string>}
            aria-label={item.title}
            className={`group/img relative block aspect-[5/4] overflow-hidden bg-black ${
              reverse ? "md:order-1" : "md:order-2"
            }`}
          >
            <Image
              src={item.imageSrc}
              alt={item.imageAlt}
              fill
              sizes="(min-width: 768px) 50vw, 100vw"
              className="object-cover transition-transform duration-700 ease-out group-hover/img:scale-[1.04]"
            />
            <div
              aria-hidden="true"
              className="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-black/40"
            />
            <span
              aria-hidden="true"
              className="absolute top-4 left-5 font-serif text-6xl leading-none font-medium tracking-tight text-white/85 [text-shadow:0_2px_20px_rgba(0,0,0,0.4)] md:text-7xl"
            >
              {item.n}
            </span>
          </Link>
        </div>
      </div>
    </section>
  );
}

function SoaBadgeGrid({
  badges,
  surface,
}: {
  badges: readonly SoaBadge[];
  surface: "panna" | "white";
}) {
  // Surface contrast: badges su panna usano bg-white; badges su white usano bg-panna.
  const cardSurface = surface === "panna" ? "bg-white" : "bg-panna";
  return (
    <div className="mt-8 grid grid-cols-1 gap-4 md:grid-cols-3 md:gap-6">
      {badges.map((badge) => (
        <div
          key={badge.code}
          className={`${cardSurface} border-border rounded-lg border px-5 py-4 md:px-6 md:py-5`}
        >
          <p className="text-brand font-[family-name:var(--font-neue-montreal)] text-xs font-semibold tracking-[0.22em] uppercase">
            {badge.code}
          </p>
          <p className="text-ink mt-1 font-serif text-lg font-medium md:text-xl">{badge.name}</p>
          <p className="text-ink/70 mt-1 text-sm leading-[1.55]">{badge.description}</p>
        </div>
      ))}
    </div>
  );
}
```

Note tecniche:

- `id={id}` sul `<section>` è MANDATORY (anchor target — verifica acceptance).
- Color token swap rispetto a ServiceOverview ServiceRow (vedi PATTERNS table lines 240-249):
  - `text-panna` → `text-ink`
  - `text-panna/65` italic numero → `text-brand` (eccezione esplicita Color §Accent reserved-for #3)
  - `text-panna/60` → `text-ink/60`
  - `text-panna/75` → `text-ink/80`
  - `text-panna/85` → `text-ink/85`
  - `border-panna/20` → `border-ink/20 hover:border-brand/50` (hover bullet pill — Color §Accent reserved-for #5)
  - `border-panna/35 hover:border-panna` → `border-ink/35 hover:border-ink`
- SOA badges sono rendered via `SoaBadgeGrid` solo se `item.soaBadges` non è vuoto. Per Privati e Professionisti il prop è `undefined` → grid omesso.
- Nessun `border-b` o `border-t` tra le righe — la separazione viene dal cambio di sfondo (panna ↔ white). NO hairline tra le sezioni (UI-SPEC § Cross-Cutting "alternating bg" semantics).
- `priority` NON applicato (UI-SPEC Asset Contract — editorial rows are below the fold).
- `font-[family-name:var(--font-neue-montreal)]` sulle SOA badge code — coerente con il pattern Phase 2 per micro-copy uppercase.
- L'eccezione `text-brand` sul codice OG1/OG2/OG3 è esplicitamente autorizzata da UI-SPEC §SOA Badge Specification ("micro-copy on neutral surface, NOT adjacent to a dark title — explicitly allowed").
  </action>

    <verify>
      <automated>pnpm typecheck && pnpm lint</automated>
    </verify>

  <acceptance_criteria> - File `components/sections/ServicesEditorialRow.tsx` esiste - `pnpm typecheck` esce con codice 0 - `pnpm lint` esce con codice 0 - `grep -E "^export function ServicesEditorialRow" components/sections/ServicesEditorialRow.tsx` ritorna 1 riga - `grep -c "use client" components/sections/ServicesEditorialRow.tsx` ritorna 0 (RSC) - `grep -c "id={id}" components/sections/ServicesEditorialRow.tsx` ritorna 1 (anchor target presente) - `grep -c "py-20 md:py-28" components/sections/ServicesEditorialRow.tsx` ritorna 1 - `grep -c "md:grid-cols-2" components/sections/ServicesEditorialRow.tsx` ritorna 1 - `grep -c "aspect-\[5/4\]" components/sections/ServicesEditorialRow.tsx` ritorna 1 - `grep -c "sizes=\"(min-width: 768px) 50vw, 100vw\"" components/sections/ServicesEditorialRow.tsx` ritorna 1 - `grep -c "group-hover/img:scale-\[1.04\]" components/sections/ServicesEditorialRow.tsx` ritorna 1 - `grep -c "group-hover/cta:translate-x-1.5" components/sections/ServicesEditorialRow.tsx` ritorna 1 - `grep -c "SoaBadgeGrid" components/sections/ServicesEditorialRow.tsx` ritorna almeno 2 (declaration + usage) - `grep -c "md:grid-cols-3" components/sections/ServicesEditorialRow.tsx` ritorna 1 (SOA badge grid) - `grep -c "rounded-lg" components/sections/ServicesEditorialRow.tsx` ritorna 1 (SOA badge) - `grep -c "from \"@/content/services\"" components/sections/ServicesEditorialRow.tsx` ritorna 1 - `grep -c "priority" components/sections/ServicesEditorialRow.tsx` ritorna 0 (no priority below-the-fold) - `grep -c "shadow-" components/sections/ServicesEditorialRow.tsx` ritorna 0 (no SaaS elevation) - `grep -c "backdrop-blur" components/sections/ServicesEditorialRow.tsx` ritorna 0 (UI-SPEC zero usage rule)
  </acceptance_criteria>

    <done>
      Componente esiste, RSC-only, type-checked, lint-clean. Renderizza `<section id={id}>` con 2-col grid alternato, body Inter su `text-ink/80`, bullet pill list su `text-ink/85` con hover brand/50, slot SoaBadgeGrid renderizzato solo quando `soaBadges` è presente. Color token swap (Phase 2 dark → Phase 3 light) applicato secondo PATTERNS table.
    </done>
  </task>

<task type="auto" tdd="false">
  <name>Task 3: Create HowWeWork.tsx (4-step grid with Lucide icons)</name>
  <files>components/sections/HowWeWork.tsx</files>

<read_first> - components/sections/Values.tsx (entire file — analog for section header pattern, light surface Eyebrow function, numbered serif italic counter pattern) - .planning/phases/03-servizi/03-PATTERNS.md ("HowWeWork" section lines 295-376 — exact prescribed JSX, icon import pattern, props shape) - .planning/phases/03-servizi/03-UI-SPEC.md ("Come lavoriamo Section Specification" lines 180-197 — surface, grid, icon size, divider rules) - .planning/phases/03-servizi/03-CONTEXT.md (D-09 4 step orizzontali con Lucide icons, D-10 stile minimalista, "Claude's Discretion" icon mapping suggestions) - content/services.ts (consume `ProcessStep` type and `servicesContent.howWeWork` shape) - package.json (confirm `lucide-react` ^1.8.0 already installed — NO new dependency)
</read_first>

  <behavior>
    `HowWeWork`:
    - RSC, no `"use client"`
    - Renderizza `<section className="bg-white text-ink py-20 md:py-28">` con container standard
    - Header left-aligned: Eyebrow + H2 serif clamp con `<em>` accent in text-brand
    - 4-col grid desktop (`md:grid-cols-4 gap-10 md:gap-12`), stack mobile (`grid-cols-1`)
    - Ogni step: serif italic "— NN" text-brand → Lucide icon size 28 strokeWidth 1.5 text-ink → H3 serif title text-ink → Inter description text-ink/70
    - Mappa icone fissa per i 4 step (basata sulla loro posizione index 0→3): ClipboardList (Analisi), Ruler (Progettazione), HardHat (Realizzazione), CheckCircle2 (Consegna)
    - Vertical hairline divider tra colonne 2-3-4 desktop (`border-l border-border`), omesso su prima colonna e su mobile
  </behavior>

  <action>
**File: `components/sections/HowWeWork.tsx`**

```tsx
// components/sections/HowWeWork.tsx
// RSC. Sezione processo a 4 step orizzontali (Analisi → Progettazione → Realizzazione → Consegna).
// Icone Lucide per ogni step (D-09). Niente animazioni — solo grid statico (D-10).
// Surface bg-white per spezzare il ritmo panna delle sezioni editoriali adiacenti.

import { ClipboardList, Ruler, HardHat, CheckCircle2 } from "lucide-react";
import type { ComponentType, SVGProps } from "react";
import type { ProcessStep } from "@/content/services";

type HowWeWorkProps = {
  eyebrow: string;
  titleStart: string;
  titleAccent: string;
  titleEnd: string;
  steps: readonly ProcessStep[];
};

// Mapping fisso per i 4 step in ordine: Analisi, Progettazione, Realizzazione, Consegna.
// Posizione index → icon component.
const STEP_ICONS: ReadonlyArray<ComponentType<SVGProps<SVGSVGElement>>> = [
  ClipboardList, // 01 — Analisi
  Ruler, // 02 — Progettazione
  HardHat, // 03 — Realizzazione
  CheckCircle2, // 04 — Consegna
];

export function HowWeWork({ eyebrow, titleStart, titleAccent, titleEnd, steps }: HowWeWorkProps) {
  return (
    <section className="text-ink bg-white py-20 md:py-28">
      <div className="mx-auto max-w-6xl px-6 md:px-12">
        <div className="mb-14 max-w-[34ch] md:mb-20">
          <Eyebrow>{eyebrow}</Eyebrow>
          <h2 className="text-ink mt-5 font-serif text-[clamp(2rem,1rem+2.6vw,3.4rem)] leading-[1.12] font-medium tracking-tight">
            {titleStart}
            <em className="text-brand font-serif italic">{titleAccent}</em>
            {titleEnd}
          </h2>
        </div>

        <div className="grid grid-cols-1 gap-10 md:grid-cols-4 md:gap-12">
          {steps.map((step, i) => {
            const Icon = STEP_ICONS[i] ?? ClipboardList;
            return (
              <div
                key={step.n}
                className="md:border-border md:border-l md:pl-6 md:first:border-l-0 md:first:pl-0"
              >
                <span className="text-brand font-serif text-base font-medium italic">
                  — {step.n}
                </span>
                <Icon aria-hidden="true" className="text-ink mt-3 h-7 w-7" strokeWidth={1.5} />
                <h3 className="text-ink mt-4 font-serif text-xl leading-tight font-medium">
                  {step.title}
                </h3>
                <p className="text-ink/70 mt-2 text-base leading-[1.6]">{step.description}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
}

function Eyebrow({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-ink/60 text-xs font-semibold tracking-[0.38em] uppercase">
      <span aria-hidden="true" className="bg-ink/40 mr-3 inline-block h-px w-8 align-middle" />
      {children}
    </p>
  );
}
```

Note tecniche:

- Import lucide-react: già installato in `package.json` come `lucide-react ^1.8.0` — NON aggiungere dipendenze.
- Icon component tipo `ComponentType<SVGProps<SVGSVGElement>>` per accettare className/strokeWidth (pattern lucide-react standard).
- `STEP_ICONS[i] ?? ClipboardList` — fallback obbligatorio per `noUncheckedIndexedAccess: true` (Phase 1 lock-in tsconfig).
- `aria-hidden="true"` sull'icona — è decorativa, il significato è veicolato dal title H3 sotto (UI-SPEC §Interaction Contracts: "step icons are NOT focusable (decorative)").
- Vertical hairline divider: `md:border-l md:pl-6 md:first:border-l-0 md:first:pl-0` — applicato solo desktop, omesso su prima colonna. Mobile stack non ha divider.
- Icon stroke `text-ink` (NOT `text-brand`) — UI-SPEC §"Come lavoriamo" line 191: "icon stays dark for legibility; brand accent is carried by the number above".
- Niente `useState`, niente `useEffect`, niente `"use client"` — sezione 100% statica.
- Eyebrow function inline (copia esatta da Values.tsx lines 61-68 + ServicesHero.tsx) — pattern duplicato deliberatamente per evitare partial abstraction in components/ui (UI-SPEC §Component Inventory line 160).
  </action>

    <verify>
      <automated>pnpm typecheck && pnpm lint</automated>
    </verify>

  <acceptance_criteria> - File `components/sections/HowWeWork.tsx` esiste - `pnpm typecheck` esce con codice 0 - `pnpm lint` esce con codice 0 - `grep -E "^export function HowWeWork" components/sections/HowWeWork.tsx` ritorna 1 riga - `grep -c "use client" components/sections/HowWeWork.tsx` ritorna 0 (RSC) - `grep -c "from \"lucide-react\"" components/sections/HowWeWork.tsx` ritorna 1 - `grep -c "ClipboardList" components/sections/HowWeWork.tsx` ritorna almeno 2 (import + usage) - `grep -c "Ruler" components/sections/HowWeWork.tsx` ritorna almeno 2 - `grep -c "HardHat" components/sections/HowWeWork.tsx` ritorna almeno 2 - `grep -c "CheckCircle2" components/sections/HowWeWork.tsx` ritorna almeno 2 - `grep -c "md:grid-cols-4" components/sections/HowWeWork.tsx` ritorna 1 - `grep -c "bg-white" components/sections/HowWeWork.tsx` ritorna 1 (section surface) - `grep -c "py-20 md:py-28" components/sections/HowWeWork.tsx` ritorna 1 - `grep -c "strokeWidth={1.5}" components/sections/HowWeWork.tsx` ritorna 1 - `grep -c "text-ink mt-3 h-7 w-7" components/sections/HowWeWork.tsx` ritorna 1 - `grep -c "text-brand font-serif" components/sections/HowWeWork.tsx` ritorna at least 2 (H2 accent + step number) - `grep -c "from \"@/content/services\"" components/sections/HowWeWork.tsx` ritorna 1 - `grep -c "aria-hidden=\"true\"" components/sections/HowWeWork.tsx` ritorna at least 1 (icon decorative) - `grep -c "?? ClipboardList" components/sections/HowWeWork.tsx` ritorna 1 (fallback per noUncheckedIndexedAccess) - `grep -c "framer-motion\|motion/react" components/sections/HowWeWork.tsx` ritorna 0 (no animation library)
  </acceptance_criteria>

    <done>
      Componente esiste, RSC-only, type-checked, lint-clean. Renderizza header left-aligned eyebrow + H2 serif accent brand italic, poi 4-col grid desktop con icone Lucide ClipboardList/Ruler/HardHat/CheckCircle2 mappate per posizione, ogni step con numero italic serif text-brand, icon text-ink, H3 serif text-ink, body Inter text-ink/70. Vertical hairline divider tra colonne 2-3-4 desktop. Niente animazioni client-side.
    </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

Phase 3 è una pagina 100% statica RSC senza form, autenticazione, fetch da API esterne, query parameter parsing, file upload, o accesso a database. Tutti i dati sono compilati in build da `content/services.ts` (TypeScript module, no runtime data). Le immagini provengono da `public/images/design/` (statiche, ottimizzate WebP). Non esistono trust boundary cross-stage che richiedano un threat model formale.

L'unica superficie di interazione utente è:

- Click su `<Link>` interni con href known-at-compile-time (`/contatti`, `/servizi#privati`, etc.)
- Hover/focus CSS su elementi statici

Nessun input utente è ricevuto, parsato, o riflesso nell'output. STRIDE register non applicabile.

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition | Mitigation Plan                                                                                                              |
| --------- | ---------------------- | ------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-03-01   | Tampering              | next/image src URLs | accept      | Tutti gli `imageSrc` provengono da `content/services.ts` (compile-time constants), no user input, no risk of path traversal. |
| T-03-02   | Information disclosure | Lucide icon library | accept      | Lucide icons sono SVG inline rendered da React, no network fetch, no telemetry. Già installato Phase 2.                      |

</threat_model>

<verification>
- `pnpm typecheck` passa
- `pnpm lint` passa
- 4 file componenti creati in `components/sections/`
- Tutti i componenti sono RSC (zero `"use client"`)
- Tutti importano dal content module Plan 01
- Pattern token swap (dark → light) applicato a ServicesEditorialRow secondo PATTERNS table
- Icone Lucide importate dal pacchetto già installato
</verification>

<success_criteria>

- 4 nuovi file in `components/sections/`: ServicesHero.tsx, TargetIndex.tsx, ServicesEditorialRow.tsx, HowWeWork.tsx
- Tutti type-checked, lint-clean, RSC
- ServicesEditorialRow accetta id, variant, item, reverse e renderizza ancora `<section id={id}>`
- TargetIndex card sono `<Link>` wrapping intero contenuto card (fully clickable)
- HowWeWork usa lucide-react (già in package.json) — zero nuove dipendenze
- SOA badge slot rendered solo per Enti Pubblici (quando `soaBadges` prop è presente)
- Implementa decisioni D-02, D-03, D-04, D-08, D-09, D-10, D-13, D-15, D-16, D-17, D-18
  </success_criteria>

<output>
Dopo completion creare `.planning/phases/03-servizi/03-02-SUMMARY.md` con:
- Lista dei 4 file creati e size
- Output di `pnpm typecheck` + `pnpm lint`
- Conferma RSC (zero "use client")
- Conferma zero nuove dipendenze in package.json
</output>
