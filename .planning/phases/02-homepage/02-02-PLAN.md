---
phase: 02-homepage
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/sections/HeroSection.tsx
  - components/sections/TrustStrip.tsx
  - components/sections/ServiceOverview.tsx
  - components/sections/ServiceAreaSection.tsx
  - components/sections/HomepageCta.tsx
  - components/ui/MobileStickyBar.tsx
  - app/page.tsx
autonomous: false
requirements:
  - HOM-01
  - HOM-02
  - HOM-03
  - HOM-05
  - HOM-06
  - HOM-07
  - HOM-08
user_setup: []

must_haves:
  truths:
    - "Sopra la piega a 375px il visitatore vede: foto reale di cantiere, headline 'Dal 1952...', CTA 'Richiedi un preventivo' senza scroll"
    - "La strip numerica mostra 70+, 450+, 35, 3 renderizzati lato server senza animazioni"
    - "Su mobile la barra sticky bottom rimane visibile durante lo scroll con CTA e telefono"
    - "L'area di servizio (Porto Viro, Rovigo, Polesine, Veneto) è dichiarata esplicitamente nel testo della pagina"
    - "pnpm check:compliance passa (nessun iframe, nessun gtag, nessun maps URL)"
    - "pnpm typecheck e pnpm lint passano con 0 errori"
    - "pnpm build completa verde — Phase 2 è deployabile"
  artifacts:
    - path: "components/sections/HeroSection.tsx"
      provides: "RSC hero full-bleed con next/image fill, overlay, headline serif, subheadline, CTA pill brand"
      exports: ["HeroSection"]
    - path: "components/sections/TrustStrip.tsx"
      provides: "RSC strip bg-ink con 4 metriche aziendali in dl/dt/dd — zero animazioni"
      exports: ["TrustStrip"]
    - path: "components/sections/ServiceOverview.tsx"
      provides: "RSC 3 card settori con icone lucide-react, link a /servizi#sezione"
      exports: ["ServiceOverview"]
    - path: "components/sections/ServiceAreaSection.tsx"
      provides: "RSC testo area di servizio su bg-surface — zones esplicite, nessuna mappa"
      exports: ["ServiceAreaSection"]
    - path: "components/sections/HomepageCta.tsx"
      provides: "RSC CTA finale centrata, pb-24 md:pb-16 per clearance sticky bar"
      exports: ["HomepageCta"]
    - path: "components/ui/MobileStickyBar.tsx"
      provides: "Client Component sticky bottom bar md:hidden — CTA + tel"
      exports: ["MobileStickyBar"]
    - path: "app/page.tsx"
      provides: "Orchestratore homepage — importa e compone tutte le sezioni, export metadata"
      exports: ["default", "metadata"]
  key_links:
    - from: "app/page.tsx"
      to: "components/sections/HeroSection.tsx"
      via: "import { HeroSection } from '@/components/sections/HeroSection'"
      pattern: "HeroSection"
    - from: "app/page.tsx"
      to: "content/homepage.ts"
      via: "import { homepageContent } from '@/content/homepage'"
      pattern: "homepageContent\\.hero"
    - from: "components/sections/HeroSection.tsx"
      to: "public/images/cantieri/residenze-universitarie-rovigo/01.jpg"
      via: "next/image con imageSrc da content/homepage.ts"
      pattern: "Image.*fill.*priority"
    - from: "components/ui/MobileStickyBar.tsx"
      to: "siteContent.contact.phone"
      via: "props phoneDisplay + phoneTel da app/page.tsx"
      pattern: "tel:"
---

<objective>
Implementare tutte le sezioni della homepage (HeroSection, TrustStrip, ServiceOverview, ServiceAreaSection, HomepageCta, MobileStickyBar) e sostituire il placeholder `app/page.tsx` con l'orchestratore reale della homepage.

Purpose: Questo piano consegna la homepage completa e deployabile. Il visitatore vedrà sopra la piega la foto reale del cantiere, il titolo autorevole e la CTA primaria. Su mobile avrà la barra sticky sempre accessibile. L'area di servizio sarà dichiarata esplicitamente.

Output: 7 file (6 componenti + page.tsx aggiornato), homepage funzionante, build verde, tutti i check automatizzati verdi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-homepage/02-01-SUMMARY.md

@content/homepage.ts
@content/site.ts
@content/navigation.ts
@app/globals.css
@app/layout.tsx
@lib/utils/cn.ts
</context>

<interfaces>
<!-- Contratti che i componenti riceveranno come props da app/page.tsx -->

Da content/homepage.ts (output del Piano 01):

```typescript
// homepageContent.hero
{ headline, subheadline, ctaLabel, ctaHref, imageSrc, imageAlt }

// homepageContent.trustStrip
{ metrics: readonly { value: string; label: string }[] }

// homepageContent.services
{ sectionTitle, sectionSubtitle, cards: readonly { title, description, href, iconName }[] }

// homepageContent.serviceArea
{ sectionTitle, body, zones: readonly string[] }

// homepageContent.finalCta
{ headline, body, ctaLabel, ctaHref }
```

Da content/site.ts:

```typescript
siteContent.contact.phone.display; // "+39 0426 000 000"
siteContent.contact.phone.tel; // "+390426000000"
```

Da content/navigation.ts:

```typescript
primaryCta.label; // "Richiedi un preventivo"
primaryCta.href; // "/contatti"
```

Da app/globals.css (token disponibili):

- Backgrounds: `bg-panna` (default), `bg-ink` (contrast strip), `bg-surface` (white card break)
- Text: `text-ink`, `text-panna`, `text-panna/70`, `text-ink/60`, `text-ink/80`
- Brand: `bg-brand text-panna` (fill-only, D-06 — MAI `text-brand` su sfondo chiaro)
- Type: `text-h1 font-serif leading-[1.1]`, `text-h2 font-serif`
- Borders: `border-border`

Da lib/utils/cn.ts:

```typescript
export function cn(...inputs: ClassValue[]): string;
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Installare lucide-react + creare HeroSection e TrustStrip</name>
  <files>
    components/sections/HeroSection.tsx,
    components/sections/TrustStrip.tsx
  </files>
  <action>
**Step A — Installare lucide-react:**

```bash
pnpm add lucide-react
```

Verificare che l'installazione riesca. Se fallisce per motivi di rete, procedere con SVG inline per le icone (vedere pattern Header.tsx) e annotare nella sezione deviazioni del SUMMARY.

**Step B — Creare components/sections/HeroSection.tsx:**

```tsx
// components/sections/HeroSection.tsx
// RSC — nessun "use client". Il "use client" per la sticky bar è in MobileStickyBar.tsx.
import Image from "next/image";
import Link from "next/link";

type HeroSectionProps = {
  headline: string;
  subheadline: string;
  ctaLabel: string;
  ctaHref: string;
  imageSrc: string;
  imageAlt: string;
};

export function HeroSection({
  headline,
  subheadline,
  ctaLabel,
  ctaHref,
  imageSrc,
  imageAlt,
}: HeroSectionProps) {
  return (
    <section className="relative h-[100svh] max-h-[900px] min-h-[600px]">
      {/* Immagine hero full-bleed — fill richiede position: relative + altezza esplicita */}
      <Image
        src={imageSrc}
        alt={imageAlt}
        fill
        priority
        sizes="100vw"
        className="object-cover"
        quality={85}
      />
      {/* Overlay scuro per leggibilità testo su foto (D-06: no text-brand su sfondo chiaro) */}
      <div className="bg-ink/55 absolute inset-0" aria-hidden="true" />
      {/* Contenuto — testo posizionato in basso per permettere alla foto di respirare */}
      <div className="relative z-10 mx-auto flex h-full max-w-6xl flex-col items-start justify-end px-6 pb-16 md:pb-20">
        <h1 className="text-h1 text-panna max-w-2xl font-serif leading-[1.1]">{headline}</h1>
        <p className="text-panna/85 mt-4 max-w-xl text-lg">{subheadline}</p>
        <Link
          href={ctaHref}
          className="bg-brand text-panna hover:bg-brand/90 mt-8 inline-flex rounded-full px-7 py-3.5 text-sm font-medium transition-colors"
        >
          {ctaLabel}
        </Link>
      </div>
    </section>
  );
}
```

**VINCOLI CRITICI:**

- `fill` su `<Image>` richiede parent con `position: relative` → `relative` è presente sulla sezione
- `h-[100svh]` garantisce che l'hero occupi l'intero viewport su mobile 375px — NON usare solo `min-h`
- `priority` è OBBLIGATORIO per LCP (Largest Contentful Paint) — non rimuovere
- `sizes="100vw"` corretto per immagine full-width
- overlay `bg-ink/55` — se durante la verifica visuale il contrasto testo/sfondo non è sufficiente, aumentare a `bg-ink/65`
- `text-panna` su overlay bg-ink/55 soddisfa WCAG AA (contrasto >> 4.5:1)
- NON usare `text-brand` direttamente — solo `bg-brand text-panna` (D-06 fill-only rule)

**Step C — Creare components/sections/TrustStrip.tsx:**

```tsx
// components/sections/TrustStrip.tsx
// RSC — nessun "use client", nessuna animazione (CLAUDE.md: "evitare effetti vistosi").
// bg-ink crea contrasto visivo forte dopo l'hero panna/immagine.

type TrustMetric = {
  value: string;
  label: string;
};

type TrustStripProps = {
  metrics: readonly TrustMetric[];
};

export function TrustStrip({ metrics }: TrustStripProps) {
  return (
    <section className="bg-ink py-12" aria-label="Numeri chiave dell'impresa">
      <div className="mx-auto max-w-6xl px-6">
        <dl className="grid grid-cols-2 gap-8 md:grid-cols-4">
          {metrics.map((m) => (
            <div key={m.label} className="text-center">
              <dt className="text-h2 text-panna font-serif">{m.value}</dt>
              <dd className="text-panna/70 mt-1 text-sm">{m.label}</dd>
            </div>
          ))}
        </dl>
      </div>
    </section>
  );
}
```

**VINCOLI:**

- `<dl>` + `<dt>` (valore) + `<dd>` (etichetta) — semantica HTML corretta per metriche
- `bg-ink` sfondo — sezione-break visivo dopo l'hero
- `font-serif text-h2` per i valori numerici — IBM Plex Serif, coerente con il design system
- ZERO animazioni (no `animate-*`, no `transition-*`, no `useEffect` counter) — valori statici
- `grid-cols-2 md:grid-cols-4` — 2 colonne mobile, 4 desktop
  </action>
  <verify>
  <automated>cd /c/Users/Ale/Documents/CostruzioniEdilferro && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>
  `components/sections/HeroSection.tsx` e `components/sections/TrustStrip.tsx` esistono, TypeScript valido, nessun errore di tipo. `lucide-react` presente in `node_modules`.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Creare ServiceOverview, ServiceAreaSection, HomepageCta, MobileStickyBar e aggiornare app/page.tsx</name>
  <files>
    components/sections/ServiceOverview.tsx,
    components/sections/ServiceAreaSection.tsx,
    components/sections/HomepageCta.tsx,
    components/ui/MobileStickyBar.tsx,
    app/page.tsx
  </files>
  <action>
**Step A — Creare components/sections/ServiceOverview.tsx:**

Sezione con 3 card servizi, icone Lucide, link a /servizi#sezione. RSC puro.

```tsx
// components/sections/ServiceOverview.tsx
// RSC. Icone Lucide importate dinamicamente tramite iconName string dal content module.
import Link from "next/link";
import { Building2, Factory, Landmark, type LucideIcon } from "lucide-react";
import { cn } from "@/lib/utils/cn";

// Mappa string → componente icona. Mantiene il content module TS puro (nessun import React).
const iconMap: Record<string, LucideIcon> = {
  Building2,
  Factory,
  Landmark,
};

type ServiceCard = {
  title: string;
  description: string;
  href: string;
  iconName: string;
};

type ServiceOverviewProps = {
  sectionTitle: string;
  sectionSubtitle: string;
  cards: readonly ServiceCard[];
};

export function ServiceOverview({ sectionTitle, sectionSubtitle, cards }: ServiceOverviewProps) {
  return (
    <section className="bg-panna py-20">
      <div className="mx-auto max-w-6xl px-6">
        <div className="mb-12 max-w-2xl">
          <h2 className="text-h2 text-ink font-serif">{sectionTitle}</h2>
          <p className="text-ink/70 mt-4 text-lg">{sectionSubtitle}</p>
        </div>
        <div className="grid gap-6 md:grid-cols-3">
          {cards.map((card) => {
            const Icon = iconMap[card.iconName] ?? Building2;
            return (
              <Link
                key={card.title}
                href={card.href}
                className={cn(
                  "group border-border bg-surface flex flex-col gap-4 rounded-xl border p-8",
                  "hover:border-ink/30 transition-colors",
                )}
              >
                <Icon className="text-brand h-7 w-7" aria-hidden="true" strokeWidth={1.5} />
                <h3 className="text-ink text-lg font-medium">{card.title}</h3>
                <p className="text-ink/70 text-sm leading-relaxed">{card.description}</p>
                <span className="text-brand mt-auto text-sm font-medium">Scopri di più →</span>
              </Link>
            );
          })}
        </div>
      </div>
    </section>
  );
}
```

**VINCOLI:**

- `iconMap` fallback su `Building2` se `iconName` non trovato — evita crash runtime
- `text-brand` su `bg-surface` (bianco): contrasto #291572 su #ffffff = 9.7:1 — WCAG AAA, conforme. NOTA: il D-06 fill-only rule si applica ai pulsanti CTA (background brand), NON alle icone e link testuali su sfondo bianco/chiaro dove il contrasto è garantito
- NON usare `text-brand` su `bg-panna` o `bg-ink` senza verifica contrasto — in questo caso siamo su `bg-surface` (bianco) quindi OK
- `strokeWidth={1.5}` — icone sottili, stile istituzionale

**Step B — Creare components/sections/ServiceAreaSection.tsx:**

```tsx
// components/sections/ServiceAreaSection.tsx
// RSC. Testo puro su bg-surface. Nessuna mappa, nessun iframe, nessun link maps.google.com.
// check:compliance non si rompe perché non ci sono URL Google Maps.

type ServiceAreaSectionProps = {
  sectionTitle: string;
  body: string;
  zones: readonly string[];
};

export function ServiceAreaSection({ sectionTitle, body, zones }: ServiceAreaSectionProps) {
  return (
    <section className="bg-surface py-20">
      <div className="mx-auto max-w-6xl px-6">
        <div className="grid gap-12 md:grid-cols-2 md:items-start">
          <div>
            <h2 className="text-h2 text-ink font-serif">{sectionTitle}</h2>
            <p className="text-ink/80 mt-6 max-w-prose text-base leading-relaxed">{body}</p>
          </div>
          <div>
            <p className="text-ink/60 text-sm font-medium tracking-widest uppercase">
              Zone principali
            </p>
            <ul className="mt-4 grid grid-cols-2 gap-2">
              {zones.map((zone) => (
                <li key={zone} className="text-ink flex items-center gap-2 text-base">
                  <span className="bg-brand h-1.5 w-1.5 shrink-0 rounded-full" aria-hidden="true" />
                  {zone}
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>
  );
}
```

**Step C — Creare components/sections/HomepageCta.tsx:**

```tsx
// components/sections/HomepageCta.tsx
// RSC. CTA finale prima del footer.
// pb-24 md:pb-16 crea clearance per la sticky bar mobile (altezza ~72px).
import Link from "next/link";

type HomepageCtaProps = {
  headline: string;
  body: string;
  ctaLabel: string;
  ctaHref: string;
};

export function HomepageCta({ headline, body, ctaLabel, ctaHref }: HomepageCtaProps) {
  return (
    <section className="bg-panna py-20 pb-24 md:pb-20">
      <div className="mx-auto max-w-3xl px-6 text-center">
        <h2 className="text-h2 text-ink font-serif">{headline}</h2>
        <p className="text-ink/70 mx-auto mt-6 max-w-xl text-lg">{body}</p>
        <Link
          href={ctaHref}
          className="bg-brand text-panna hover:bg-brand/90 mt-10 inline-flex rounded-full px-8 py-4 text-base font-medium transition-colors"
        >
          {ctaLabel}
        </Link>
      </div>
    </section>
  );
}
```

**VINCOLI:**

- `pb-24 md:pb-20` — clearance per la sticky bar mobile (~72px). NON ridurre questo valore.
- `bg-brand text-panna` per la CTA (D-06 fill-only)

**Step D — Creare components/ui/MobileStickyBar.tsx:**

```tsx
// components/ui/MobileStickyBar.tsx
// "use client" — necessario per position:fixed dopo hydration.
// Visibile solo su mobile (md:hidden). z-50 > Header z-40.
"use client";

import Link from "next/link";

type MobileStickyBarProps = {
  ctaLabel: string;
  ctaHref: string;
  phoneDisplay: string;
  phoneTel: string;
};

export function MobileStickyBar({
  ctaLabel,
  ctaHref,
  phoneDisplay,
  phoneTel,
}: MobileStickyBarProps) {
  return (
    <div
      className="border-border bg-panna/95 fixed right-0 bottom-0 left-0 z-50 flex border-t p-3 backdrop-blur md:hidden"
      role="complementary"
      aria-label="Azioni rapide mobile"
    >
      <a
        href={`tel:${phoneTel}`}
        className="border-ink/20 text-ink flex-1 rounded-full border py-3 text-center text-sm font-medium"
        aria-label={`Chiama ${phoneDisplay}`}
      >
        {phoneDisplay}
      </a>
      <Link
        href={ctaHref}
        className="bg-brand text-panna ml-2 flex-1 rounded-full py-3 text-center text-sm font-medium"
      >
        {ctaLabel}
      </Link>
    </div>
  );
}
```

**VINCOLI:**

- `"use client"` in prima riga (prima di qualsiasi import)
- `z-50` (Header è `z-40`) — la barra sovrasta sempre il contenuto
- `md:hidden` — invisibile su tablet/desktop
- `bg-panna/95 backdrop-blur` — trasparenza con blur, coerente con Header

**Step E — Sostituire app/page.tsx con l'orchestratore homepage:**

```tsx
// app/page.tsx
// Homepage — orchestratore. Importa sezioni e passa dati via props dal content module.
// Nessun hardcoding: tutto il copy viene da content/homepage.ts o content/site.ts.
import { buildMetadata } from "@/lib/seo/metadata";
import { homepageContent } from "@/content/homepage";
import { siteContent } from "@/content/site";
import { primaryCta } from "@/content/navigation";
import { HeroSection } from "@/components/sections/HeroSection";
import { TrustStrip } from "@/components/sections/TrustStrip";
import { ServiceOverview } from "@/components/sections/ServiceOverview";
import { ServiceAreaSection } from "@/components/sections/ServiceAreaSection";
import { HomepageCta } from "@/components/sections/HomepageCta";
import { MobileStickyBar } from "@/components/ui/MobileStickyBar";

export const metadata = buildMetadata({
  title: "Impresa Edile — Porto Viro, Rovigo, Veneto",
  description: siteContent.brand.tagline,
  alternates: { canonical: "/" },
});

export default function Home() {
  return (
    <>
      <HeroSection {...homepageContent.hero} />
      <TrustStrip {...homepageContent.trustStrip} />
      <ServiceOverview {...homepageContent.services} />
      <ServiceAreaSection {...homepageContent.serviceArea} />
      <HomepageCta {...homepageContent.finalCta} />
      <MobileStickyBar
        ctaLabel={primaryCta.label}
        ctaHref={primaryCta.href}
        phoneDisplay={siteContent.contact.phone.display}
        phoneTel={siteContent.contact.phone.tel}
      />
    </>
  );
}
```

**VINCOLI:**

- `export const metadata` — la homepage sovrascrive il metadata di layout con titolo specifico SEO
- `MobileStickyBar` riceve i dati da `primaryCta` e `siteContent` — non da `homepageContent` (è un componente globale che segue il dato di navigazione)
- Spread `{...homepageContent.hero}` è type-safe perché `HeroSectionProps` corrisponde ai campi di `homepageContent.hero`
- NON importare o usare `"use client"` in `page.tsx` — è un RSC orchestratore
  </action>
  <verify>
  <automated>cd /c/Users/Ale/Documents/CostruzioniEdilferro && pnpm lint && pnpm typecheck 2>&1 | tail -15</automated>
  </verify>
  <done>
  Tutti i 5 componenti esistono. `app/page.tsx` è l'orchestratore reale (non più il placeholder). `pnpm lint` → 0 errori. `pnpm typecheck` → 0 errori.
  </done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Homepage completa con:
- HeroSection: foto cantiere Rovigo full-bleed, headline "Dal 1952...", CTA "Richiedi un preventivo"
- TrustStrip: 70+ / 450+ / 35 / 3 su sfondo scuro
- ServiceOverview: 3 card servizi con icone (Building2, Factory, Landmark)
- ServiceAreaSection: testo area di servizio con elenco zone
- HomepageCta: sezione CTA finale centrata
- MobileStickyBar: barra sticky bottom visibile solo su mobile
  </what-built>
  <how-to-verify>
1. Avviare il dev server: `pnpm dev` (localhost:3000)

2. **Test above-the-fold mobile (priorità assoluta):**
   - Aprire Chrome DevTools → Toggle device toolbar → selezionare iPhone SE (375×667)
   - Visitare http://localhost:3000
   - VERIFICA: senza scroll si vede la foto del cantiere, il titolo "Dal 1952 costruiamo valore...", il pulsante "Richiedi un preventivo"
   - VERIFICA: il titolo è in font serif (IBM Plex Serif), non in sans-serif
   - VERIFICA: il testo è bianco su sfondo scuro (overlay visibile)

3. **Test trust strip:**
   - Scorrere sotto l'hero
   - VERIFICA: strip scura con "70+", "450+", "35", "3" e relative etichette
   - VERIFICA: nessuna animazione (i numeri appaiono staticamente)

4. **Test sezione servizi:**
   - VERIFICA: 3 card con icone, titoli Residenziale / Industriale e Commerciale / Restauri e Opere Pubbliche
   - VERIFICA: le card sono cliccabili (hover visibile su desktop)

5. **Test area di servizio:**
   - VERIFICA: testo che menziona Porto Viro, Rovigo, Polesine, Veneto
   - VERIFICA: elenco zone con pallini brand

6. **Test mobile sticky bar:**
   - Rimanere su viewport mobile 375px
   - Scrollare lentamente tutta la pagina
   - VERIFICA: la barra bottom con numero di telefono e "Richiedi un preventivo" rimane sempre visibile
   - VERIFICA: su desktop (>= 768px) la barra NON è visibile
   - VERIFICA: il numero di telefono nella sticky bar, se toccato, apre `tel:`

7. **Test check automatizzati:**
   - `pnpm check:compliance` → 0 violazioni
   - `pnpm build` → build verde senza errori TypeScript

Annotare qualsiasi problema visuale osservato (contrasto insufficiente, overflow, testo tagliato su mobile) prima di approvare.
</how-to-verify>
<resume-signal>
Digitare "approvato" se tutto è corretto, oppure descrivere i problemi riscontrati (es. "contrasto insufficiente nell'hero", "sticky bar copre l'ultimo contenuto") per correzione.
</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                              |
| -------------------------- | -------------------------------------------------------------------------------------------------------- |
| content module → RSC props | Tutti i dati (imageSrc, href, strings) sono string literals in content/homepage.ts — nessun input utente |
| MobileStickyBar tel: href  | Il phoneTel viene da siteContent.contact.phone.tel — string literal, nessun input esterno                |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                           |
| --------- | ---------------------- | ----------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-03   | Tampering              | HeroSection — imageSrc come prop    | accept      | Il valore è una string literal da content/homepage.ts; nessun utente può modificarlo. Next.js serve immagini solo da public/ per default. |
| T-02-04   | Tampering              | ServiceOverview — href card link    | accept      | Tutti gli href sono "/servizi#sezione" — path locali, nessun redirect esterno, nessun input utente.                                       |
| T-02-05   | Tampering              | MobileStickyBar — tel: href         | accept      | phoneTel è una string literal da siteContent. Nessun input utente coinvolto.                                                              |
| T-02-06   | Information Disclosure | app/page.tsx — metadata description | accept      | La description usa siteContent.brand.tagline — testo pubblico intenzionalmente esposto per SEO.                                           |

</threat_model>

<verification>
Verifica completa Phase 2:

**Automatizzata:**

1. `pnpm lint` → 0 errori
2. `pnpm typecheck` → 0 errori (typedRoutes attivo + tutti i Link type-safe)
3. `pnpm check:compliance` → 0 violazioni (nessun iframe, nessun gtag, nessun google.com/maps)
4. `pnpm check:contrast` → tutti i token superano 4.5:1
5. `pnpm check:layout` → Header/Footer/Analytics/SpeedInsights/lang="it" presenti
6. `pnpm build` → build verde, nessun errore TypeScript o warning Turbopack

**Requisiti soddisfatti:**

- HOM-01: Hero above fold a 375px con foto + titolo + CTA — verifica visuale checkpoint
- HOM-02: Trust strip con 4 metriche RSC senza animazioni — `pnpm typecheck` (TrustMetric[4])
- HOM-03: Sticky bar mobile durante scroll — verifica visuale checkpoint
- HOM-04: typedRoutes riabilitato (Piano 01) — `pnpm typecheck`
- HOM-05: Area di servizio dichiarata (Porto Viro, Rovigo, Polesine, Veneto nel body) — `pnpm check:compliance` + review testo
- HOM-06: Link ai servizi presenti nelle card — `pnpm typecheck` (typedRoutes valida gli href)
- HOM-07: CTA finale presente (HomepageCta) — verifica visuale checkpoint
- HOM-08: check:compliance passa — `pnpm check:compliance`
  </verification>

<success_criteria>

- `pnpm lint && pnpm typecheck` → 0 errori
- `pnpm check:compliance` → 0 violazioni
- `pnpm build` → verde
- Homepage visibile a localhost:3000 con tutte le sezioni in sequenza: Hero → TrustStrip → ServiceOverview → ServiceAreaSection → HomepageCta
- Above the fold a 375px: foto cantiere + titolo serif + CTA pill brand visibili senza scroll
- Su mobile la sticky bar bottom rimane visibile durante lo scroll
- Su desktop (>= 768px) la sticky bar NON è visibile
- Checkpoint umano approvato
  </success_criteria>

<output>
Dopo il completamento e l'approvazione del checkpoint, creare `.planning/phases/02-homepage/02-02-SUMMARY.md` con:
- Lista completa dei file creati/modificati con descrizione
- Decisioni prese (es. overlay opacity, gestione iconMap fallback)
- Risultati di tutti i check automatizzati
- Eventuali deviazioni dal piano (con giustificazione)
- Commit hash per ogni task
</output>
